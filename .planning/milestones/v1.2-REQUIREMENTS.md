# Requirements Archive: v1.2 internationalization with next-intl

**Planned:** 2026-04-30
**Status:** PLANNED

Milestone v1.2 adds site-level internationalization using `next-intl` and ships Italian (`it`) and French (`fr`) translations for recruiter-facing portfolio content.

Requirements (I18N):

- I18N-01: Add `next-intl` dependency and TypeScript types to project.
- I18N-02: Configure Next.js locale routing and server provider (root `layout`) to load translations server-side.
- I18N-03: Extract UI strings into translation keys and wire `useTranslations` (server/client where appropriate).
- I18N-04: Add translation files for `it` and `fr` covering all recruiter-facing copy (hero, experience, contact, sections).
- I18N-05: Implement language switcher and locale-aware links; add `hreflang` meta/link tags for SEO.
- I18N-06: Update test suite (Playwright, node regression) to validate localized pages and fallback locale behavior.
