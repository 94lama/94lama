---
id: 260413-m3q
type: quick-plan
mode: quick
autonomous: true
files_modified:
  - app/layout.tsx
  - public/assets/icon.png
---

# Quick Plan — Generate PNG fallback for public/assets/readme/icon.svg and wire it as favicon fallback

## Objective

Add a PNG favicon fallback while keeping the existing SVG favicon in place and leaving `public/assets/readme/icon.svg` unchanged.

## Context

- The root layout currently points favicon metadata to `/assets/readme/icon.svg`.
- An existing PNG asset is present at `public/assets/icon.png` and can be used as the fallback.
- The user explicitly requested that `public/assets/readme/icon.svg` must not be changed.

## Tasks

### Task 1 — Wire PNG fallback into metadata

Files:
- app/layout.tsx
- public/assets/icon.png

Action:
- Update `metadata.icons` to include both the SVG icon and the PNG fallback.
- Keep the SVG as the primary icon reference and add the PNG as an additional compatible icon/shortcut entry.
- Stage the existing `public/assets/icon.png` file as part of the favicon asset set.

Verify:
- `app/layout.tsx` references both `/assets/readme/icon.svg` and `/assets/icon.png`.
- `public/assets/readme/icon.svg` is untouched.

Done:
- The app exposes both SVG and PNG favicon metadata and the PNG fallback asset is committed.

### Task 2 — Quick docs/state bookkeeping

Files:
- .planning/quick/260413-m3q-generate-png-fallback-for-public-assets-/260413-m3q-PLAN.md
- .planning/quick/260413-m3q-generate-png-fallback-for-public-assets-/260413-m3q-SUMMARY.md
- .planning/STATE.md

Action:
- Create summary.
- Add the quick task to `STATE.md`, creating the Quick Tasks Completed section if missing.
- Update the Last activity lines.

## Success criteria

- SVG favicon remains unchanged.
- PNG fallback is wired in metadata and tracked in git.
- Quick task artifacts exist and `STATE.md` is updated.
