---
quick_task: 260412-gm7
type: quick
autonomous: true
files_modified:
  - app/components/knowledge-experience-coordinator.tsx
---

<objective>
Make the section `01` knowledge map use the full content width instead of staying constrained to the right-side column.

Purpose: Improve map readability and scanning by giving the primary skills surface the same horizontal space as the experience timeline beneath it.
Output: Updated layout in the shared knowledge/experience coordinator so the map renders full width.
</objective>

<context>
@.planning/STATE.md
@./AGENTS.md
@app/components/knowledge-experience-coordinator.tsx
@app/components/skills-knowledge-map.tsx

<interfaces>
From `app/components/knowledge-experience-coordinator.tsx`:
```tsx
<section className="grid gap-8 lg:grid-cols-[minmax(0,0.72fr)_minmax(0,1.28fr)] ...">
  <SectionHeading ... />
  <SkillsKnowledgeMap ... />
</section>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Let the section-01 map span the full row width</name>
  <files>app/components/knowledge-experience-coordinator.tsx</files>
  <action>Refactor the section `01` layout in `KnowledgeExperienceCoordinator` so the `SectionHeading` sits above the map instead of consuming the left grid column beside it. Keep the existing section copy and the overall page rhythm, but make the `SkillsKnowledgeMap` span the full available content width for that section on large screens as well as smaller breakpoints. Do not change the shared selection behavior, timeline wiring, or map internals for this quick task.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The section `01` heading still appears before the knowledge map, and the map now renders full width instead of being constrained to the right-hand grid column.</done>
</task>

</tasks>

<success_criteria>
- The section `01` knowledge map spans the full content width available to that section.
- The heading remains present and readable above the map.
- No map interaction or timeline wiring changes.
- `npm run build` passes after the layout update.
</success_criteria>

<output>
After completion, create `.planning/quick/260412-gm7-put-the-knowledge-map-width-to-full-now-/260412-gm7-SUMMARY.md`
</output>
