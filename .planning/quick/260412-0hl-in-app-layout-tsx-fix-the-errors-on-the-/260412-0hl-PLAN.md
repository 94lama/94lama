---
quick_task: 260412-0hl
type: quick
autonomous: true
files_modified:
  - app/layout.tsx
  - app/components/legal-embed-footer.tsx
---

<objective>
Fix the errors triggered from `app/layout.tsx` around cookie/privacy embed handling without changing the existing privacy-policy and cookie-policy tags themselves.

Purpose: Restore a valid Next.js root layout while preserving the exact third-party legal embed markup the user explicitly locked.
Output: A layout-safe integration boundary for the existing legal footer embed plus a clean `app/layout.tsx` that builds without the current errors.
</objective>

<context>
@.planning/STATE.md
@./AGENTS.md
@app/layout.tsx

<interfaces>
From `app/layout.tsx`:
```tsx
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Isolate the legal embed from the root layout without altering its markup</name>
  <files>app/layout.tsx, app/components/legal-embed-footer.tsx</files>
  <action>Read the relevant Next.js guidance in `node_modules/next/dist/docs/` before editing, per `AGENTS.md`. Refactor `app/layout.tsx` so the footer’s Iubenda privacy/cookie embed no longer causes root-layout or server/client execution errors. Preserve the legal tags exactly as user-locked content: same hrefs, classes, titles, visible labels, and script bodies. Do not rewrite, sanitize, or substitute those tags; instead, fix the integration around them by moving them behind the correct rendering boundary if needed (for example, a dedicated client-side footer embed component or an equivalent Next-safe wrapper).</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>`app/layout.tsx` no longer contains the invalid pattern causing the current privacy/cookie embed errors, and the legal embed content itself remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Validate cookie/privacy behavior still loads through the new boundary</name>
  <files>app/layout.tsx, app/components/legal-embed-footer.tsx</files>
  <action>Ensure the new boundary still lets the third-party script initialize in the browser so the privacy-policy and cookie-policy links continue to function, while avoiding direct server-side access to browser-only globals or invalid inline-script placement in the layout. Keep the rest of the layout structure, metadata, fonts, and footer styling intact unless a minimal wiring change is required to clear the errors.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The app builds successfully, the root layout remains structurally unchanged aside from the embed integration fix, and the legal footer can initialize client-side without editing the locked tags.</done>
</task>

</tasks>

<success_criteria>
- `app/layout.tsx` no longer throws the current cookie/privacy embed errors.
- The Iubenda privacy and cookie tags remain textually unchanged.
- Any browser-only execution is moved behind a safe client/runtime boundary.
- `npm run lint` and `npm run build` pass after the fix.
</success_criteria>

<output>
After completion, create `.planning/quick/260412-0hl-in-app-layout-tsx-fix-the-errors-on-the-/260412-0hl-SUMMARY.md`
</output>
