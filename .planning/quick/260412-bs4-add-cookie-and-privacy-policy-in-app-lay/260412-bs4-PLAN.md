---
quick_task: 260412-bs4
type: quick
autonomous: true
files_modified:
  - app/layout.tsx
  - app/components/legal-embed-footer.tsx
---

<objective>
Add the cookie-policy and privacy-policy integration to the app root layout using `app/components/cookie-policy.html` and `app/components/privacy-policy.html` as the source of truth.

Purpose: Make the legal policy links/scripts load from the new snippet files through a Next 16 App Router-safe layout integration.
Output: A minimal root-layout wiring change that keeps the legal embed working without pulling unrelated deleted `public/*` files back into scope.
</objective>

<context>
@.planning/STATE.md
@./AGENTS.md
@app/layout.tsx
@app/components/legal-embed-footer.tsx
@app/components/cookie-policy.html
@app/components/privacy-policy.html
@node_modules/next/dist/docs/01-app/02-guides/scripts.md

<interfaces>
From `app/layout.tsx`:
```tsx
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>)
```

From `app/components/legal-embed-footer.tsx`:
```tsx
export function LegalEmbedFooter()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the root layout host the legal embed through a Next-safe boundary</name>
  <files>app/layout.tsx, app/components/legal-embed-footer.tsx</files>
  <action>Use the Next 16 script guidance from `node_modules/next/dist/docs/01-app/02-guides/scripts.md` before editing. Keep `app/components/cookie-policy.html` and `app/components/privacy-policy.html` as the source of truth for the policy anchor/script content, and wire the root layout to render that content through `LegalEmbedFooter` in a way that is safe for the App Router. Preserve the existing metadata, font setup, and body structure in `app/layout.tsx`. Do not inject raw HTML directly into the layout, do not recreate `public/cookie-anchor.html` or `public/privacy-anchor.html`, and do not touch those unrelated deletions unless a hard blocker proves they are required.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>`app/layout.tsx` remains valid Next App Router layout code, and the legal footer is wired from the two `app/components/*.html` snippets instead of any `public/*` anchor files.</done>
</task>

<task type="auto">
  <name>Task 2: Validate the cookie/privacy snippets still initialize correctly</name>
  <files>app/layout.tsx, app/components/legal-embed-footer.tsx</files>
  <action>Confirm the footer still reads the two snippet files, exposes the privacy and cookie links, and loads the third-party Iubenda script in a way that builds cleanly in Next 16. Keep the change minimal: only adjust the layout/footer integration and any strictly necessary wiring around the current snippet reader. Leave the untracked snippet files as the canonical source, and keep unrelated worktree changes out of scope.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The app builds successfully, the root layout includes the legal policy integration, and the footer continues to derive both policy links/scripts from `app/components/cookie-policy.html` and `app/components/privacy-policy.html`.</done>
</task>

</tasks>

<success_criteria>
- `app/layout.tsx` stays Next 16 App Router-safe.
- Cookie and privacy policy integration uses `app/components/cookie-policy.html` and `app/components/privacy-policy.html` as the only source of truth.
- No unrelated `public/cookie-anchor.html` or `public/privacy-anchor.html` restoration is required.
- `npm run lint` and `npm run build` pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260412-bs4-add-cookie-and-privacy-policy-in-app-lay/260412-bs4-SUMMARY.md`
</output>
