---
+quick_task: 260412-k0k
+type: quick
+autonomous: true
+files_modified:
+  - app/layout.tsx
---

<objective>
Add the requested Iubenda widget script to the root layout header path using Next.js' `next/script` pattern instead of raw head markup.

Purpose: Load the third-party bootstrap globally in an App Router-safe way while keeping the change isolated to `app/layout.tsx`.
Output: A minimal root-layout update that adds `https://embeds.iubenda.com/widgets/283fdcdd-8702-47d7-84ee-59bc1203b52c.js` through `Script`.
</objective>

<context>
@.planning/STATE.md
@./AGENTS.md
@TODO.md
@app/layout.tsx
@node_modules/next/dist/docs/01-app/02-guides/scripts.md

<interfaces>
From `app/layout.tsx`:
```tsx
import Script from "next/script";

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the global Iubenda widget script with Next Script</name>
  <files>app/layout.tsx</files>
  <action>Follow the Next App Router script guidance in `node_modules/next/dist/docs/01-app/02-guides/scripts.md` and add the requested third-party asset to the root layout with `next/script`, not a raw `<head>` tag and not a literal HTML `<script>` node pasted into JSX. Reuse the existing `Script` import already present in `app/layout.tsx`. Add `src="https://embeds.iubenda.com/widgets/283fdcdd-8702-47d7-84ee-59bc1203b52c.js"` as a global application script, keep the change scoped to this file, and preserve the current metadata, font setup, legal links/scripts, and analytics wiring unless a minimal reordering is required for valid layout structure.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>`app/layout.tsx` includes the requested Iubenda widget via `next/script`, and the layout still follows App Router-safe script loading guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the root layout still builds with the added script</name>
  <files>app/layout.tsx</files>
  <action>Validate that the new global script coexists cleanly with the existing inline Iubenda loaders and GTM entries already in `app/layout.tsx`. Keep this quick task atomic: do not refactor unrelated footer/legal behavior, and do not expand scope into the broader cookie bootstrap TODO beyond inserting the requested widget script.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>The app builds successfully after the script addition, with no new layout/script errors introduced by the global Iubenda widget include.</done>
</task>

</tasks>

<success_criteria>
- `app/layout.tsx` loads the requested Iubenda widget through `next/script`.
- No raw `<head>` insertion is introduced.
- Existing layout behavior stays intact outside this targeted script addition.
- `npm run lint` and `npm run build` remain green.
</success_criteria>

<output>
After completion, create `.planning/quick/260412-k0k-add-script-type-text-javascript-src-http/260412-k0k-SUMMARY.md`
</output>
