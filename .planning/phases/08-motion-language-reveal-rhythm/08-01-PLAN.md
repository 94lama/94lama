---
phase: 08-motion-language-reveal-rhythm
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/components/section-card-styles.ts
  - app/components/section-shell.tsx
  - app/components/section-heading.tsx
  - app/components/hero-section.tsx
  - app/components/knowledge-experience-coordinator.tsx
autonomous: true
requirements:
  - MOTN-01
  - MOTN-02
user_setup: []
must_haves:
  truths:
    - Shared reveal primitives exist in reusable app code and are consumed by the hero plus section-level wrappers.
    - Hero, numbered sections, and the skills or experience wrappers use one restrained fade-up reveal family instead of mixed ad-hoc entrance styles.
    - Recruiter-facing content remains immediately available to read, especially in the hero, without fake loading or long sequencing.
  artifacts:
    - path: app/components/section-card-styles.ts
      provides: Shared motion constants or class exports for reveal and interaction timing.
      contains: motion
    - path: app/components/section-shell.tsx
      provides: Shared section-shell reveal adoption.
      contains: reveal
    - path: app/components/hero-section.tsx
      provides: Hero surfaces wired to the shared reveal system.
      contains: reveal
  key_links:
    - from: app/components/section-shell.tsx
      to: app/components/section-card-styles.ts
      via: Shared reveal or motion exports are applied through the reusable section shell.
      pattern: "section.*motion|reveal|motion"
    - from: app/components/hero-section.tsx
      to: app/components/section-card-styles.ts
      via: Hero reveal or timing classes come from the shared motion source instead of bespoke values.
      pattern: "motion|reveal"
---

<objective>
Create the shared motion primitives and reveal entry points for Phase 8 so the page can use one restrained fade-up rhythm without delaying recruiter reading.

Purpose: Phase 8 needs one reusable motion language before surface-specific interaction polish can align to it.
Output: Shared motion tokens or helpers plus reveal adoption across the hero, static section shell, and the skills or experience wrapper sections.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-motion-language-reveal-rhythm/08-CONTEXT.md
@.planning/phases/08-motion-language-reveal-rhythm/08-RESEARCH.md
@.planning/phases/08-motion-language-reveal-rhythm/08-UI-SPEC.md
@app/globals.css
@app/components/section-card-styles.ts
@app/components/section-shell.tsx
@app/components/section-heading.tsx
@app/components/hero-section.tsx
@app/components/knowledge-experience-coordinator.tsx
@app/components/knowledge-map/runtime.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define one shared motion token source for reveals and compact interaction timing</name>
  <files>app/globals.css, app/components/section-card-styles.ts</files>
  <read_first>app/globals.css, app/components/section-card-styles.ts, app/components/knowledge-map/runtime.ts, .planning/phases/08-motion-language-reveal-rhythm/08-CONTEXT.md</read_first>
  <action>Extend the existing motion baseline instead of adding new tooling. Keep `fade-in-up` as the shared reveal direction in `app/globals.css`, and add any needed motion CSS variables, utility classes, or exported class strings in `app/components/section-card-styles.ts` so section shells, headings, cards, and CTA surfaces can all read from the same timing family. Reuse the current compact feel already present in the card and chip classes: short durations, subtle translate values, and no cinematic sequencing. Do not add a new animation dependency, fake loading states, or scroll-driven effects.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `app/components/section-card-styles.ts` contains shared motion exports or class strings for reveal and interaction timing.
    - `app/globals.css` still contains `@keyframes fade-in-up` and does not add a second conflicting entrance direction.
    - `package.json` does not gain a new animation dependency.
  </acceptance_criteria>
  <done>Reusable motion primitives exist in the shared styling layer and compile successfully with the current Next.js and Tailwind setup.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the shared reveal rhythm to hero and section-level wrappers</name>
  <files>app/components/section-shell.tsx, app/components/section-heading.tsx, app/components/hero-section.tsx, app/components/knowledge-experience-coordinator.tsx</files>
  <read_first>app/components/section-shell.tsx, app/components/section-heading.tsx, app/components/hero-section.tsx, app/components/knowledge-experience-coordinator.tsx, app/components/section-card-styles.ts, .planning/phases/08-motion-language-reveal-rhythm/08-UI-SPEC.md</read_first>
  <action>Wire the shared reveal rhythm into the page entry surfaces. Update `SectionShell` so numbered static sections can adopt one reusable reveal behavior. Apply the same family to `SectionHeading` and to the `KnowledgeExperienceCoordinator` wrappers for sections `01` and `02`, since they do not currently use `SectionShell`. In `hero-section.tsx`, add reveal polish only to supporting hero surfaces and layout groups while keeping the main recruiter copy and email CTA effectively immediate. Use small delays only for nearby child groups when necessary, and keep the reveal direction aligned to the existing fade-up baseline.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `app/components/section-shell.tsx` contains shared reveal usage for static numbered sections.
    - `app/components/knowledge-experience-coordinator.tsx` contains reveal-related classes or styles on both the skills and experience section wrappers.
    - `app/components/hero-section.tsx` contains shared reveal usage without removing the existing email CTA link or hero text content.
  </acceptance_criteria>
  <done>The hero and section wrappers visibly share one restrained reveal system in code while preserving immediate recruiter readability and current content structure.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build
- [ ] gsd-tools verify references passes for this plan file
- [ ] Hero, static sections, and the skills or experience wrappers all read from the same reveal family in code.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Shared motion primitives exist in reusable code rather than per-section duplication.
- The hero remains recruiter-first and effectively immediate while still gaining restrained reveal polish.
</success_criteria>

<output>
After completion, create the phase 8 plan 01 summary file in this phase directory.
</output>
