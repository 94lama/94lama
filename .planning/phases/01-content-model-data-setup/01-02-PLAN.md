---
phase: 01-content-model-data-setup
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/page.tsx
autonomous: true
requirements:
  - CONT-02
must_haves:
  truths:
    - The landing page renders from the shared portfolio content source instead of embedded starter copy.
    - Recruiter-facing content sections render without breaking the current single-page app behavior.
    - The page reflects the current CV content automatically when the source file changes.
  artifacts:
    - path: app/page.tsx
      provides: Server-rendered one-page consumer of the shared portfolio content.
      exports:
        - default
  key_links:
    - from: app/page.tsx
      to: src/content/portfolio/get-portfolio-content.ts
      via: async server component import
      pattern: getPortfolioContent
---

<objective>
Replace the starter-template page content with a simple recruiter-readable rendering that consumes the shared portfolio content source built in Plan 01.

Purpose: Satisfy CONT-02 and prove the Phase 1 content foundation works end-to-end before Phase 2 introduces the bolder portfolio UI.
Output: `app/page.tsx` rendered from `getPortfolioContent()` with all required v1 sections present.
</objective>

<execution_context>
@/workspaces/94lama/.opencode/get-shit-done/workflows/execute-plan.md
@/workspaces/94lama/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-content-model-data-setup/01-CONTEXT.md
@.planning/phases/01-content-model-data-setup/01-RESEARCH.md
@.planning/phases/01-content-model-data-setup/01-VALIDATION.md
@app/page.tsx
@public/assets/cv.md

<interfaces>
This plan consumes the shared content API created in Plan 01:

```ts
export interface PortfolioContent {
  hero: { name: string; role: string };
  summary: string;
  skills: Array<{ category: string; items: string[] }>;
  experience: Array<{
    role: string;
    company: string;
    dateRange: string;
    highlights: string[];
  }>;
  education: string[];
  languages: Array<{ label: string; level: string }>;
  relocation: { summary: string; preferredRegions?: string[] };
  contact: {
    email: string;
    phone?: string;
    location: string;
    github?: string;
    linkedin?: string;
  };
  projects?: Array<{ name: string; description?: string }>;
}

export async function getPortfolioContent(): Promise<PortfolioContent>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor the page to render from shared portfolio content</name>
  <read_first>
    - app/page.tsx
    - src/content/portfolio/get-portfolio-content.ts
    - src/content/portfolio/types.ts
    - public/assets/cv.md
    - .planning/phases/01-content-model-data-setup/01-CONTEXT.md
  </read_first>
  <files>app/page.tsx</files>
  <action>Rewrite `app/page.tsx` as an async server component that imports `getPortfolioContent()` and renders the returned data into a straightforward single-page structure. Replace the default Next/Vercel starter content entirely. Render these sections from live data: hero (name + role), summary, grouped skills, professional experience with company/date/highlights, education, languages, relocation, and contact. Keep the presentation intentionally simple and content-first for Phase 1 per D-04 and the phase boundary; do not introduce the bold Phase 2 design work yet, and do not add a visible project showcase because projects are deferred. If `projects` exists in the shared model, it may remain unused in the page for this phase.</action>
  <acceptance_criteria>
    - `app/page.tsx` contains `await getPortfolioContent()`.
    - `app/page.tsx` no longer contains `To get started, edit the page.tsx file.`.
    - `app/page.tsx` no longer imports `next/image` for the starter logos.
    - `app/page.tsx` contains visible section labels or headings for `Summary`, `Skills`, `Experience`, `Education`, `Languages`, `Relocation`, and `Contact`.
    - `app/page.tsx` renders at least one `.map(` over shared content arrays.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "await getPortfolioContent\(|Summary|Skills|Experience|Education|Languages|Relocation|Contact|\.map\(" app/page.tsx &amp;&amp; ! rg -n "To get started, edit the page.tsx file\.|next/image|/next.svg|vercel.com/new" app/page.tsx</automated>
  </verify>
  <done>The home page is a functioning consumer of the shared content source and no longer contains hardcoded starter-template copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared portfolio data → page render | Parsed portfolio strings are presented in the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `app/page.tsx` | mitigate | Render values from typed fields only and avoid injecting raw markdown/HTML. |
| T-01-05 | D | `app/page.tsx` | mitigate | Consume the shared content contract directly so missing sections fail during build/typecheck rather than silently diverging from the source. |
| T-01-06 | I | contact rendering | accept | Contact data is intentionally public portfolio information; no additional secrecy controls are needed in this phase. |
</threat_model>

<verification>
- `npm run lint`
- `npm run build`
- `rg -n "await getPortfolioContent\(|Summary|Skills|Experience|Education|Languages|Relocation|Contact" app/page.tsx`
- `! rg -n "To get started, edit the page.tsx file\.|/next.svg|vercel.com/new" app/page.tsx`
</verification>

<success_criteria>
- `app/page.tsx` imports and awaits `getPortfolioContent()`.
- The page renders the required v1 sections from shared content instead of hardcoded starter copy.
- The app remains buildable after the Phase 1 content-source refactor.
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-model-data-setup/01-content-model-data-setup-02-SUMMARY.md`
</output>
