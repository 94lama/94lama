---
phase: 04-knowledge-map-experience-contract
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/content/portfolio/rank-experience-by-selection.ts
  - tests/phase-04-knowledge-experience.test.ts
autonomous: true
requirements:
  - EXP-01
  - EXP-02
must_haves:
  truths:
    - "A single knowledge-map selection produces deterministic match scores for every experience entry."
    - "No selection or unmatched selection preserves the full experience timeline instead of hiding entries."
    - "Matched entries sort ahead of nonmatches without losing stable original ordering for ties."
  artifacts:
    - path: "src/content/portfolio/rank-experience-by-selection.ts"
      provides: "Pure helper that derives helper copy, highlighting, and stable ordering from one selection input"
      exports:
        - "rankExperienceBySelection"
        - "type RankedExperienceEntry"
    - path: "tests/phase-04-knowledge-experience.test.ts"
      provides: "Automated proof for ranking, fallback, and stable-order behavior"
      contains: "rankExperienceBySelection"
  key_links:
    - from: "tests/phase-04-knowledge-experience.test.ts"
      to: "src/content/portfolio/rank-experience-by-selection.ts"
      via: "Node test import"
      pattern: "rankExperienceBySelection"
---

<objective>
Lock the shared map-to-experience derivation contract before any section restructuring.

Purpose: Prevent map/experience drift by giving later UI work one tested source of truth for highlight, helper-copy, and reorder behavior.
Output: A pure ranking helper plus phase-specific automated tests.
</objective>

<execution_context>
@/workspaces/94lama/.opencode/get-shit-done/workflows/execute-plan.md
@/workspaces/94lama/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-knowledge-map-experience-contract/04-RESEARCH.md
@.planning/phases/04-knowledge-map-experience-contract/04-VALIDATION.md
@app/components/experience-map-controller.tsx
@app/components/experience-map-section.tsx
@app/components/skills-knowledge-map.tsx
@src/content/portfolio/types.ts
@tests/phase-03-contact-validation.test.ts

<interfaces>
From `src/content/portfolio/types.ts`:
```ts
export interface SkillGroup {
  category: string;
  items: string[];
  entries: SkillEntry[];
}

export interface ExperienceEntry {
  role: string;
  company: string;
  dateRange: string;
  highlights: string[];
}
```

From `app/components/skills-knowledge-map.tsx`:
```ts
export type KnowledgeMapSelection = {
  id: string;
  label: string;
  kind: 'core' | 'category' | 'skill';
  activeIndex: number;
};
```
</interfaces>
</context>

<feature>
  <name>Shared ranking contract for knowledge-map selection</name>
  <files>src/content/portfolio/rank-experience-by-selection.ts, tests/phase-04-knowledge-experience.test.ts</files>
  <behavior>
    Case 1: `core` selection returns every experience entry in original order with fallback helper copy.
    Case 2: skill selection promotes matching entries to the top, marks them highlighted, and keeps nonmatching entries after them.
    Case 3: category selection uses the active group's `items` list as match terms.
    Case 4: unmatched selections still return the full timeline with explanatory helper copy instead of an empty list.
    Case 5: ties preserve original CV order as the stable tiebreaker.
  </behavior>
  <implementation>Export a pure helper that accepts `{ experience, selection, skillGroups }` and returns ordered entries plus helper metadata. Normalize text safely, avoid browser APIs, and keep the helper independent of React so later client components can memoize around it.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: Write failing tests and helper contract</name>
  <files>tests/phase-04-knowledge-experience.test.ts, src/content/portfolio/rank-experience-by-selection.ts</files>
  <read_first>
    - tests/phase-03-contact-validation.test.ts
    - app/components/experience-map-section.tsx
    - app/components/skills-knowledge-map.tsx
    - src/content/portfolio/types.ts
  </read_first>
  <action>Create `tests/phase-04-knowledge-experience.test.ts` using the existing Node test pattern from Phase 3. Write failing tests for `core`, `skill`, `category`, and unmatched selections, plus stable-order behavior. In `src/content/portfolio/rank-experience-by-selection.ts`, add only the exported types and function signature first: `rankExperienceBySelection({ experience, selection, skillGroups })` plus a `RankedExperienceEntry` type that includes `entry`, `matchScore`, `isHighlighted`, `matchedTerms`, and `originalIndex`.</action>
  <acceptance_criteria>
    - `tests/phase-04-knowledge-experience.test.ts` contains `test(` entries for `core`, `skill`, `category`, and unmatched selection behavior.
    - `src/content/portfolio/rank-experience-by-selection.ts` exports `rankExperienceBySelection`.
    - The first test run fails because implementation is still incomplete.
  </acceptance_criteria>
  <verify>
    <automated>node --test --experimental-strip-types tests/phase-04-knowledge-experience.test.ts</automated>
  </verify>
  <done>Phase 4 test scaffold exists, imports compile, and at least one assertion fails for the intended RED step.</done>
</task>

<task type="auto">
  <name>Task 2: Implement pure ranking and fallback logic</name>
  <files>src/content/portfolio/rank-experience-by-selection.ts, tests/phase-04-knowledge-experience.test.ts</files>
  <read_first>
    - src/content/portfolio/rank-experience-by-selection.ts
    - tests/phase-04-knowledge-experience.test.ts
    - app/components/experience-map-section.tsx
    - app/components/skills-knowledge-map.tsx
  </read_first>
  <action>Implement normalization and scoring so the helper derives selection terms from `KnowledgeMapSelection`: `skill` uses the selected label, `category` uses `skillGroups[activeIndex].items`, and `core` uses no match terms. Return every experience entry with a numeric `matchScore`, boolean `isHighlighted`, matched term list, and stable ordering (`matchScore` descending, `originalIndex` ascending as tiebreaker). Generate helper copy for three states only: overview/core, matched results, and unmatched fallback. Do not filter entries out of the returned array.</action>
  <acceptance_criteria>
    - `src/content/portfolio/rank-experience-by-selection.ts` contains `matchScore` and `originalIndex` in the returned structure.
    - The helper never returns a shorter list than the input `experience.length`.
    - Helper copy includes one unmatched fallback string explaining the full timeline remains visible.
    - Phase 4 tests pass green after implementation.
  </acceptance_criteria>
  <verify>
    <automated>node --test --experimental-strip-types tests/phase-04-knowledge-experience.test.ts</automated>
  </verify>
  <done>The ranking helper is pure, deterministic, and fully covered by automated tests for the required selection states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| knowledge-map selection → ranking helper | Untrusted UI selection data crosses into derived ordering logic |
| authored CV content → ranking helper | Text content from `cv.md` is parsed and matched against selection terms |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | T | `rankExperienceBySelection` | mitigate | Normalize selection terms, clamp `activeIndex` through safe access, and fall back to overview mode when selection data is invalid instead of trusting arbitrary IDs or indexes |
| T-4-02 | D | `rankExperienceBySelection` | mitigate | Keep helper pure and linear over the provided arrays; avoid recursive graph walks or unbounded regex behavior so malformed content cannot degrade rendering work |
| T-4-03 | I | helper copy output | accept | Output contains only already-public portfolio content and derived labels; no secret or user data crosses this boundary |
</threat_model>

<verification>
- `node --test --experimental-strip-types tests/phase-04-knowledge-experience.test.ts`
- Confirm the helper can be imported without React or browser-only dependencies.
</verification>

<success_criteria>
- A reusable ranking helper exists under `src/content/portfolio/`.
- Automated tests prove `core`, matched, and unmatched behavior without hiding experience entries.
- Later UI plans can consume a stable typed contract instead of duplicating selection logic.
</success_criteria>

<output>
After completion, create `.planning/phases/04-knowledge-map-experience-contract/04-01-SUMMARY.md`
</output>
