---
phase: 04-knowledge-map-experience-contract
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - app/page.tsx
  - app/components/knowledge-experience-coordinator.tsx
  - app/components/experience-timeline-section.tsx
  - app/components/experience-card.tsx
  - app/components/skills-knowledge-map.tsx
  - app/components/experience-map-controller.tsx
  - app/components/experience-map-section.tsx
  - tests/phase-04-knowledge-experience.test.ts
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - EXP-01
  - EXP-02
must_haves:
  truths:
    - "Recruiters use the section `01` knowledge map as the primary skills surface instead of a separate skills grid."
    - "Selecting a map node or category updates the section `02` experience timeline immediately from the same client state."
    - "All experience entries remain visible while matching entries highlight and rise to the top."
    - "The rendered knowledge map no longer shows a visible center sphere."
  artifacts:
    - path: "app/components/knowledge-experience-coordinator.tsx"
      provides: "Single shared state owner for sections `01` and `02`"
      exports:
        - "KnowledgeExperienceCoordinator"
    - path: "app/components/experience-timeline-section.tsx"
      provides: "Synced full-timeline renderer driven by ranking helper"
      exports:
        - "ExperienceTimelineSection"
    - path: "app/components/skills-knowledge-map.tsx"
      provides: "Primary section-01 map UI without a rendered center sphere"
      exports:
        - "SkillsKnowledgeMap"
    - path: "app/page.tsx"
      provides: "Server-first page composition that mounts the shared coordinator and removes duplicate surfaces"
      contains: "KnowledgeExperienceCoordinator"
  key_links:
    - from: "app/page.tsx"
      to: "app/components/knowledge-experience-coordinator.tsx"
      via: "component import and JSX mount"
      pattern: "KnowledgeExperienceCoordinator"
    - from: "app/components/knowledge-experience-coordinator.tsx"
      to: "src/content/portfolio/rank-experience-by-selection.ts"
      via: "derived experience ordering"
      pattern: "rankExperienceBySelection"
    - from: "app/components/knowledge-experience-coordinator.tsx"
      to: "app/components/skills-knowledge-map.tsx"
      via: "controlled selection props"
      pattern: "selectedNodeId|onSelectionChange"
---

<objective>
Restructure sections `01` and `02` around one shared knowledge-map interaction model and update the map UI to match the new contract.

Purpose: Deliver the recruiter-facing Phase 4 outcome — one primary map surface, one synced full timeline, and no duplicate interactive block.
Output: Shared coordinator components, updated page composition, and refined map rendering/layout.
</objective>

<execution_context>
@/workspaces/94lama/.opencode/get-shit-done/workflows/execute-plan.md
@/workspaces/94lama/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-knowledge-map-experience-contract/04-RESEARCH.md
@.planning/phases/04-knowledge-map-experience-contract/04-VALIDATION.md
@app/page.tsx
@app/components/experience-map-controller.tsx
@app/components/experience-map-section.tsx
@app/components/skills-knowledge-map.tsx
@src/content/portfolio/types.ts
@src/content/portfolio/rank-experience-by-selection.ts
@tests/phase-04-knowledge-experience.test.ts

<interfaces>
From `app/components/skills-knowledge-map.tsx`:
```ts
export type KnowledgeMapSelection = {
  id: string;
  label: string;
  kind: 'core' | 'category' | 'skill';
  activeIndex: number;
};

type SkillsKnowledgeMapProps = {
  skillGroups: SkillGroup[];
  activeIndex?: number;
  selectedNodeId?: string;
  onSelectionChange?: (selection: KnowledgeMapSelection) => void;
};
```

From `src/content/portfolio/rank-experience-by-selection.ts` (created by Plan 01):
```ts
export type RankedExperienceEntry = {
  entry: ExperienceEntry;
  matchScore: number;
  isHighlighted: boolean;
  matchedTerms: string[];
  originalIndex: number;
};

export function rankExperienceBySelection(args: {
  experience: ExperienceEntry[];
  selection: KnowledgeMapSelection;
  skillGroups: SkillGroup[];
}): {
  entries: RankedExperienceEntry[];
  helperCopy: string;
  isFallback: boolean;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared coordinator and full-timeline renderer</name>
  <files>app/components/knowledge-experience-coordinator.tsx, app/components/experience-timeline-section.tsx, app/components/experience-card.tsx</files>
  <read_first>
    - app/components/experience-map-controller.tsx
    - app/components/experience-map-section.tsx
    - app/components/skills-knowledge-map.tsx
    - src/content/portfolio/rank-experience-by-selection.ts
    - src/content/portfolio/types.ts
  </read_first>
  <action>Create `KnowledgeExperienceCoordinator` as the only `'use client'` owner of selection state for sections `01` and `02`. Initialize it with the non-visual root selection `{ id: 'core', label: 'Knowledge Graph', kind: 'core', activeIndex: skillGroups[0] ? 0 : -1 }`, pass controlled props into `SkillsKnowledgeMap`, and derive timeline output by calling `rankExperienceBySelection`. Create `ExperienceTimelineSection` to render helper copy plus the full ordered list, and create `ExperienceCard` to render one experience entry with class-level emphasis from `isHighlighted` and `matchScore`. Keep all text rendered as plain strings; do not introduce `dangerouslySetInnerHTML`.</action>
  <acceptance_criteria>
    - `app/components/knowledge-experience-coordinator.tsx` contains `useMemo(` with `rankExperienceBySelection`.
    - `app/components/knowledge-experience-coordinator.tsx` contains the root selection object with `id: 'core'`.
    - `app/components/experience-timeline-section.tsx` maps over every ranked entry and does not filter nonmatches out.
    - `app/components/experience-card.tsx` accepts highlight/emphasis props and renders `entry.role`, `entry.company`, and `entry.highlights`.
  </acceptance_criteria>
  <verify>
    <automated>node --test --experimental-strip-types tests/phase-04-knowledge-experience.test.ts</automated>
  </verify>
  <done>One shared client coordinator exists and can drive both the map and the full experience timeline from the same selection state.</done>
</task>

<task type="auto">
  <name>Task 2: Replace duplicate skills and interactive experience sections in the page shell</name>
  <files>app/page.tsx, app/components/experience-map-controller.tsx, app/components/experience-map-section.tsx</files>
  <read_first>
    - app/page.tsx
    - app/components/knowledge-experience-coordinator.tsx
    - app/components/experience-map-controller.tsx
    - app/components/experience-map-section.tsx
    - .planning/ROADMAP.md
  </read_first>
  <action>Update `app/page.tsx` so the server component removes the static section `01` skills grid, removes the static section `02` experience list, deletes the `02A` “Interactive Skills And Experience” block, and mounts `<KnowledgeExperienceCoordinator skillGroups={content.skills} experience={content.experience} />` in their place. Keep the single-page flow intact by placing the coordinator in the same page position between the hero and education/languages sections. Delete `app/components/experience-map-controller.tsx` and `app/components/experience-map-section.tsx` after the new coordinator owns this behavior; no imports, JSX, or comments may reference those retired components.</action>
  <acceptance_criteria>
    - `app/page.tsx` contains `KnowledgeExperienceCoordinator`.
    - `app/page.tsx` no longer contains `ExperienceMapController`.
    - `app/page.tsx` no longer contains the string `Interactive Skills And Experience`.
    - `app/page.tsx` no longer renders the old static skills card grid from `content.skills.map(`.
    - `app/components/experience-map-controller.tsx` and `app/components/experience-map-section.tsx` are removed or empty placeholders are not left behind.
  </acceptance_criteria>
  <verify>
    <automated>node --test --experimental-strip-types tests/phase-04-knowledge-experience.test.ts</automated>
  </verify>
  <done>The page exposes only one Phase 4 skills/experience interaction surface and the duplicate `02A` block is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Refine the map renderer for the new section-01 role</name>
  <files>app/components/skills-knowledge-map.tsx, tests/phase-04-knowledge-experience.test.ts</files>
  <read_first>
    - app/components/skills-knowledge-map.tsx
    - app/components/knowledge-experience-coordinator.tsx
    - tests/phase-04-knowledge-experience.test.ts
    - .planning/phases/04-knowledge-map-experience-contract/04-RESEARCH.md
  </read_first>
  <action>Refactor `skills-knowledge-map.tsx` so the semantic `core` selection still exists in data/state but the rendered scene and projected hit-target list skip the visible center sphere. Increase category and skill spacing by pushing category orbits farther apart and increasing per-skill Y/Z variance so clusters scan more clearly without shrinking tap targets. Keep the public controlled API unchanged (`activeIndex`, `selectedNodeId`, `onSelectionChange`). Update inspector and helper copy so the component reads as the primary section `01` skills surface, not as a separate experimental widget. Extend `tests/phase-04-knowledge-experience.test.ts` with assertions that protect the page wiring and visible-center-node regression.</action>
  <acceptance_criteria>
    - `app/components/skills-knowledge-map.tsx` still contains `id: 'core'` in selection/data logic.
    - The render path no longer creates a visible node mesh for `node.kind === 'core'`.
    - `app/components/skills-knowledge-map.tsx` still exports `KnowledgeMapSelection` and `SkillsKnowledgeMap` with the same controlled prop names.
    - `tests/phase-04-knowledge-experience.test.ts` contains assertions covering removal of the duplicate `02A` page block and protection of the `core` fallback contract.
  </acceptance_criteria>
  <verify>
    <automated>node --test --experimental-strip-types tests/phase-03-contact-validation.test.ts tests/phase-04-knowledge-experience.test.ts && npm run build</automated>
  </verify>
  <done>The knowledge map is the primary section-01 skills UI, the center sphere is no longer rendered, and the full page still builds with synced section-02 behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-loaded portfolio content → client coordinator | Serialized content crosses from `app/page.tsx` into the client island |
| client selection state → timeline rendering | Untrusted map interactions drive ordering and highlight presentation |
| client OGL scene → pointer hit testing | Browser pointer input controls visual selection changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-04 | T | `KnowledgeExperienceCoordinator` | mitigate | Accept only `KnowledgeMapSelection` values produced by controlled map/category handlers, preserve the `core` fallback, and derive presentation solely through `rankExperienceBySelection` instead of ad hoc branch logic |
| T-4-05 | I | `ExperienceCard` / timeline rendering | mitigate | Render authored CV strings as plain React text nodes and keep `dangerouslySetInnerHTML` out of the new coordinator/timeline files |
| T-4-06 | D | `skills-knowledge-map.tsx` pointer picking | mitigate | Keep projected-node radii bounded and ignore off-screen nodes during selection so drag/tap handling cannot create runaway selection work |
| T-4-07 | R | page restructuring | accept | This is local UI state with no audit trail requirement; summary and tests are sufficient for traceability at ASVS L1 |
</threat_model>

<verification>
- `node --test --experimental-strip-types tests/phase-03-contact-validation.test.ts tests/phase-04-knowledge-experience.test.ts && npm run build`
- Read `app/page.tsx` and confirm section `02A` no longer exists.
- Read `app/components/skills-knowledge-map.tsx` and confirm `core` remains semantic state, not a rendered center sphere.
</verification>

<success_criteria>
- Section `01` is now the knowledge map, not a static skills grid.
- Section `02` stays fully visible while reacting immediately to map selection.
- The duplicate interactive block is removed.
- The map keeps the hidden root/default state but no longer renders a center sphere.
</success_criteria>

<output>
After completion, create `.planning/phases/04-knowledge-map-experience-contract/04-02-SUMMARY.md`
</output>
