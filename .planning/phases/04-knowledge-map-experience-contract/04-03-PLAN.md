---
phase: 04-knowledge-map-experience-contract
plan: "03"
type: execute
wave: 3
depends_on:
  - 04-02
files_modified: []
autonomous: false
requirements:
  - MAP-01
  - MAP-02
  - EXP-01
  - EXP-02
must_haves:
  truths:
    - "A human can confirm the section `01` map is readable without a center sphere."
    - "A human can confirm section `02` keeps every experience entry visible while matched entries move to the top."
    - "A human can confirm the duplicate skills and `02A` interactive surfaces are gone."
  artifacts:
    - path: "app/page.tsx"
      provides: "Single page flow with one shared knowledge-map/experience surface"
      contains: "KnowledgeExperienceCoordinator"
    - path: "app/components/skills-knowledge-map.tsx"
      provides: "Readable de-centered map UI for section `01`"
      exports:
        - "SkillsKnowledgeMap"
  key_links:
    - from: "Human verification"
      to: "Phase 4 UI behavior"
      via: "local browser run"
      pattern: "section 01 map + section 02 synced timeline"
---

<objective>
Pause for final human verification of the map legibility and synced timeline behavior.

Purpose: Phase 4 includes visual interaction requirements that need a quick browser confirmation after automation passes.
Output: Approved or rejected checkpoint feedback for the execute workflow.
</objective>

<execution_context>
@/workspaces/94lama/.opencode/get-shit-done/workflows/execute-plan.md
@/workspaces/94lama/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-knowledge-map-experience-contract/04-RESEARCH.md
@.planning/phases/04-knowledge-map-experience-contract/04-VALIDATION.md
@app/page.tsx
@app/components/knowledge-experience-coordinator.tsx
@app/components/skills-knowledge-map.tsx
@app/components/experience-timeline-section.tsx
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify section-01 map legibility and section-02 sync</name>
  <read_first>
    - app/page.tsx
    - app/components/knowledge-experience-coordinator.tsx
    - app/components/skills-knowledge-map.tsx
    - app/components/experience-timeline-section.tsx
  </read_first>
  <action>Run the app after Plan 02 automation completes, inspect the updated homepage in a browser, and confirm the visual/interaction requirements that cannot be fully proven by CLI tests alone: section `01` uses the knowledge map as the primary skills surface, the map no longer renders a center sphere, section `02` keeps every experience entry visible while matched entries rise and highlight, and the duplicate `02A` interactive block is gone.</action>
  <what-built>Section `01` now uses the knowledge map as the primary skills surface, section `02` reacts to the same selection state, the duplicate `02A` block is removed, and the map no longer shows a visible center sphere.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the homepage.
    2. Confirm section `01` shows the knowledge map instead of the old static skills card grid.
    3. Confirm there is no separate `02A` “Interactive Skills And Experience” section anywhere on the page.
    4. In section `01`, confirm the map has no visible center sphere and the nodes are spaced clearly enough to scan on desktop and a narrow mobile viewport.
    5. Click at least one category chip and one skill node; confirm section `02` updates immediately.
    6. Confirm all experience entries remain visible after each selection, with related entries moved higher and visually emphasized rather than isolated alone.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms the old static skills grid is gone.
    - Human confirms the center sphere is gone.
    - Human confirms the duplicate `02A` block is gone.
    - Human confirms section `02` stays fully visible while matched entries rise/highlight.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — human browser verification required after `npm run dev` serves the updated page</automated>
  </verify>
  <resume-signal>Type "approved" or describe the issues to fix.</resume-signal>
  <done>Human approval confirms the Phase 4 interaction contract is not only implemented but readable and trustworthy in the browser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered browser UI → human validation | Visual interaction quality is judged in a live browser, beyond static code checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-08 | E | browser-only interaction quality | mitigate | Require an explicit human verification checkpoint before phase completion so unreadable spacing or misleading reorder behavior cannot ship on passing tests alone |
</threat_model>

<verification>
- Manual browser pass from the checkpoint steps above.
- If issues are found, return them verbatim so the next execution loop can create gap-closure or revisions.
</verification>

<success_criteria>
- Human confirms the map is readable and de-centered.
- Human confirms section `02` remains fully visible and synced.
- Human confirms the duplicate surfaces are removed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-knowledge-map-experience-contract/04-03-SUMMARY.md`
</output>
