---
phase: 10-responsive-hierarchy-rewrite
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page.tsx
  - app/components/hero-section.tsx
  - app/components/knowledge-experience-coordinator.tsx
  - app/components/section-shell.tsx
  - app/components/section-card-styles.ts
  - app/components/layout/page-rhythm.ts
  - app/components/layout/responsive-section-grid.tsx
autonomous: true
requirements:
  - LAY-01
  - LAY-02
  - LAY-03
user_setup: []
must_haves:
  truths:
    - The page uses shared server-safe layout recipes to create clearer section rhythm instead of ad hoc per-section spacing.
    - The hero hands off more directly into the skills and experience proof block while preserving the current DOM order.
    - Mobile, tablet, and desktop layouts improve scan comfort through CSS-first reflow rather than viewport-driven client logic.
  artifacts:
    - path: app/components/layout/page-rhythm.ts
      provides: Shared spacing and section rhythm recipes for the responsive hierarchy rewrite.
      contains: rhythm
    - path: app/components/layout/responsive-section-grid.tsx
      provides: Reusable server-safe grouping wrapper for responsive lower-section composition.
      contains: ResponsiveSectionGrid
    - path: app/page.tsx
      provides: Reworked top-level page composition with clearer hero-to-proof hierarchy.
      contains: KnowledgeExperienceCoordinator
    - path: app/components/hero-section.tsx
      provides: Updated positioning and CTA composition that remains recruiter-first.
      contains: Email me
  key_links:
    - from: app/page.tsx
      to: app/components/layout/page-rhythm.ts
      via: The page root consumes shared rhythm exports instead of hardcoding all breakpoint spacing inline.
      pattern: rhythm|page
    - from: app/page.tsx
      to: app/components/layout/responsive-section-grid.tsx
      via: The page uses a reusable responsive grouping helper for lower-section pairings.
      pattern: ResponsiveSectionGrid
    - from: app/page.tsx
      to: app/components/knowledge-experience-coordinator.tsx
      via: The proof block remains directly after the hero in the page composition.
      pattern: HeroSection|KnowledgeExperienceCoordinator
---

<objective>
Create the shared page-rhythm foundation and primary composition rewrite for Phase 10 so the portfolio reads faster through clearer spacing, proof-first handoff, and CSS-first responsive structure.

Purpose: Phase 10 needs the most disruptive hierarchy work isolated first so the page-level composition becomes clearer before supporting sections are compacted and verification guardrails are updated.
Output: Shared layout recipes plus a reworked `app/page.tsx`, hero, and proof-block composition that preserve DOM order while improving hierarchy across breakpoints.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-responsive-hierarchy-rewrite/10-CONTEXT.md
@.planning/phases/10-responsive-hierarchy-rewrite/10-RESEARCH.md
@.planning/phases/10-responsive-hierarchy-rewrite/10-UI-SPEC.md
@.planning/phases/09-interactive-continuity-real-loading-states/09-CONTEXT.md
@app/page.tsx
@app/components/hero-section.tsx
@app/components/knowledge-experience-coordinator.tsx
@app/components/section-shell.tsx
@app/components/section-card-styles.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared server-safe layout recipes for page rhythm and responsive grouping</name>
  <files>app/components/layout/page-rhythm.ts, app/components/layout/responsive-section-grid.tsx, app/components/section-shell.tsx, app/components/section-card-styles.ts</files>
  <read_first>app/components/section-shell.tsx, app/components/section-card-styles.ts, app/page.tsx, .planning/phases/10-responsive-hierarchy-rewrite/10-RESEARCH.md, .planning/phases/10-responsive-hierarchy-rewrite/10-UI-SPEC.md</read_first>
  <action>Create a small `app/components/layout/` surface for Phase 10. Add `page-rhythm.ts` that exports concrete class recipes for page shell spacing, proof-block spacing, and supporting-grid spacing across mobile, tablet, and desktop. Add `responsive-section-grid.tsx` as a server-safe wrapper that accepts concrete variant values for lower-section pairings instead of using client viewport logic. Update `section-shell.tsx` and `section-card-styles.ts` only as needed to support comfortable versus compact density variants, shared internal spacing, and section-level rhythm hooks that the page and supporting sections can reuse. Keep all layout decisions CSS-first and do not introduce `window.innerWidth`, `matchMedia`, or extra client boundaries for layout behavior.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `app/components/layout/page-rhythm.ts` exists and exports reusable page-rhythm values or helpers.
    - `app/components/layout/responsive-section-grid.tsx` exists and exports `ResponsiveSectionGrid`.
    - `app/components/section-shell.tsx` or `app/components/section-card-styles.ts` contains explicit density or rhythm support rather than only the previous fixed spacing values.
  </acceptance_criteria>
  <done>Phase 10 has one reusable, server-safe layout foundation for spacing rhythm and responsive grouping instead of relying on scattered page-local class tuning.</done>
</task>

<task type="auto">
  <name>Task 2: Recompose the page shell, hero, and proof block around a clearer proof-first handoff</name>
  <files>app/page.tsx, app/components/hero-section.tsx, app/components/knowledge-experience-coordinator.tsx, app/components/layout/page-rhythm.ts, app/components/layout/responsive-section-grid.tsx</files>
  <read_first>app/page.tsx, app/components/hero-section.tsx, app/components/knowledge-experience-coordinator.tsx, .planning/phases/10-responsive-hierarchy-rewrite/10-CONTEXT.md, .planning/phases/10-responsive-hierarchy-rewrite/10-UI-SPEC.md</read_first>
  <action>Update `app/page.tsx` to consume the new rhythm and grouping helpers while preserving the existing DOM order: `HeroSection`, then `KnowledgeExperienceCoordinator`, then the education/languages and relocation/contact groups. Rework the page wrapper spacing so the hero and proof block feel more connected than the lower supporting sections. In `hero-section.tsx`, rebalance the grid, internal spacing, support-card grouping, and CTA cluster so the recruiter sees positioning, summary, and the primary email action first, with relocation and profile/support details clearly secondary. In `knowledge-experience-coordinator.tsx`, tighten the relationship between the skills and experience sections so they read as one proof block with stronger hierarchy than the lower grids. Use concrete CSS-first grid, gap, width, and padding recipes from the new layout helpers; do not reorder DOM nodes, do not hide the existing CTA links, and do not introduce new loading or transition behavior beyond carrying forward the current Phase 8 and 9 rules.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `app/page.tsx` still renders `HeroSection` before `KnowledgeExperienceCoordinator` and uses `ResponsiveSectionGrid` or shared rhythm exports for the lower groups.
    - `app/components/hero-section.tsx` still contains `href={\`mailto:${contact.email}\`}` and `Email me` while using updated composition classes for hierarchy and spacing.
    - `app/components/knowledge-experience-coordinator.tsx` contains page-rhythm-related spacing updates that keep the skills and experience sections grouped as the primary proof block.
  </acceptance_criteria>
  <done>The page reads more clearly from positioning into proof across breakpoints while preserving the current recruiter-safe DOM order and server-first rendering shape.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build
- [ ] node ".opencode/get-shit-done/bin/gsd-tools.cjs" verify references ".planning/phases/10-responsive-hierarchy-rewrite/10-01-PLAN.md"
- [ ] The page uses shared layout recipes and keeps the proof block directly after the hero without DOM-order drift.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| Server page composition to recruiter-facing layout | Page rhythm and grouping recipes determine what recruiters see first across breakpoints | Layout classes, grouping variants |
| Shared section primitives to page shell | Density and rhythm variants propagate into reusable section wrappers | Section spacing and card-surface presentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `page-rhythm.ts` / `responsive-section-grid.tsx` | mitigate | Keep the new layout helpers server-safe and reusable so later edits do not fragment the hierarchy rewrite into conflicting per-section spacing rules. |
| T-10-02 | Spoofing | `app/page.tsx` / `hero-section.tsx` composition | mitigate | Preserve the real recruiter reading order and primary CTA wiring so the revised layout does not create a visually clever but semantically misleading scan path. |
</threat_model>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Shared layout recipes drive page rhythm and responsive grouping.
- The hero hands off more directly into the proof block while preserving source order and CTA visibility.

</success_criteria>

<output>
After completion, create the phase 10 plan 01 summary file in this phase directory.
</output>
