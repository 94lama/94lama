---
phase: 10-responsive-hierarchy-rewrite
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/components/education-section.tsx
  - app/components/languages-section.tsx
  - app/components/relocation-section.tsx
  - app/components/contact-section.tsx
  - app/components/section-shell.tsx
  - app/components/loading/route-shell-skeleton.tsx
  - tests/phase-04-knowledge-experience.test.ts
  - package.json
autonomous: true
requirements:
  - LAY-01
  - LAY-02
  - LAY-03
user_setup: []
must_haves:
  truths:
    - Supporting sections feel compact and clearly secondary to the hero and proof block while remaining readable.
    - The contact section keeps the primary email CTA obvious across the revised lower-page composition.
    - Automated verification covers the new hierarchy contracts, including route-shell alignment and DOM-order-safe composition cues.
  artifacts:
    - path: app/components/contact-section.tsx
      provides: Updated lower-page CTA composition with the primary email action still obvious.
      contains: Primary CTA
    - path: app/components/loading/route-shell-skeleton.tsx
      provides: Route-level loading shell aligned to the revised page structure.
      contains: Hero loading shell
    - path: tests/phase-04-knowledge-experience.test.ts
      provides: Source assertions for responsive hierarchy and layout guardrails.
      contains: KnowledgeExperienceCoordinator
  key_links:
    - from: app/components/contact-section.tsx
      to: app/page.tsx
      via: Lower-page composition still presents the primary contact action as a visible destination in the revised hierarchy.
      pattern: ContactSection|Primary CTA|Email me
    - from: tests/phase-04-knowledge-experience.test.ts
      to: app/page.tsx
      via: Tests assert the page keeps the hero-first then proof-first section order and responsive grouping helpers.
      pattern: HeroSection|KnowledgeExperienceCoordinator|ResponsiveSectionGrid
    - from: tests/phase-04-knowledge-experience.test.ts
      to: app/components/loading/route-shell-skeleton.tsx
      via: Tests verify the route shell still mirrors the revised page structure instead of drifting out of sync.
      pattern: Hero loading shell|Interactive section loading shell|Lower content grids loading shell
---

<objective>
Compact and rebalance the supporting sections, keep lower-page CTA visibility explicit, and lock the responsive hierarchy rewrite into the automated verification surface.

Purpose: After the primary page composition lands, Phase 10 still needs the lower sections tuned to the new hierarchy and protected by source-level guardrails so the rewrite does not regress quietly.
Output: Supporting-section density updates, route-shell alignment, and Node tests that verify the new hierarchy contracts.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-responsive-hierarchy-rewrite/10-CONTEXT.md
@.planning/phases/10-responsive-hierarchy-rewrite/10-RESEARCH.md
@.planning/phases/10-responsive-hierarchy-rewrite/10-UI-SPEC.md
@.planning/phases/10-responsive-hierarchy-rewrite/10-01-PLAN.md
@app/components/education-section.tsx
@app/components/languages-section.tsx
@app/components/relocation-section.tsx
@app/components/contact-section.tsx
@app/components/loading/route-shell-skeleton.tsx
@tests/phase-04-knowledge-experience.test.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compact and rebalance the lower supporting sections for the new hierarchy</name>
  <files>app/components/education-section.tsx, app/components/languages-section.tsx, app/components/relocation-section.tsx, app/components/contact-section.tsx, app/components/section-shell.tsx, app/components/loading/route-shell-skeleton.tsx</files>
  <read_first>app/components/education-section.tsx, app/components/languages-section.tsx, app/components/relocation-section.tsx, app/components/contact-section.tsx, app/components/section-shell.tsx, app/components/loading/route-shell-skeleton.tsx, .planning/phases/10-responsive-hierarchy-rewrite/10-UI-SPEC.md</read_first>
  <action>Using the density and rhythm foundation from Plan 01, update the lower supporting sections so they feel clearly secondary to the hero and proof block. Tune `education-section.tsx`, `languages-section.tsx`, and `relocation-section.tsx` toward compact, scannable card groupings with cleaner internal spacing and breakpoint behavior. Update `contact-section.tsx` so the primary email CTA remains the strongest action in the lower page while secondary actions stay close and do not out-compete it. Adjust `section-shell.tsx` if a compact lower-section variant is needed across these sections. If the revised structure materially changes lower-page geometry, update `app/components/loading/route-shell-skeleton.tsx` so the route-level shell still mirrors the real page regions and hierarchy. Keep all changes CSS-first, preserve existing mailto/link wiring, and do not introduce sticky UI or fake loading behavior.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `app/components/contact-section.tsx` still contains `Primary CTA` and `href={\`mailto:${contact.email}\`}`.
    - At least one of `app/components/education-section.tsx`, `app/components/languages-section.tsx`, or `app/components/relocation-section.tsx` contains updated compact layout classes that differ from the previous fixed spacing shape.
    - `app/components/loading/route-shell-skeleton.tsx` remains present and still contains the hero, interactive-section, and lower-grid shell labels if it is updated.
  </acceptance_criteria>
  <done>The lower sections reinforce trust and contact discoverability without competing with the proof-first hierarchy established in Plan 01.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the existing Node tests with Phase 10 hierarchy guardrails</name>
  <files>tests/phase-04-knowledge-experience.test.ts, package.json</files>
  <read_first>tests/phase-04-knowledge-experience.test.ts, package.json, app/page.tsx, app/components/hero-section.tsx, app/components/contact-section.tsx, app/components/loading/route-shell-skeleton.tsx, .planning/phases/10-responsive-hierarchy-rewrite/10-RESEARCH.md</read_first>
  <action>Extend `tests/phase-04-knowledge-experience.test.ts` with concrete Phase 10 source assertions. Add checks that `app/page.tsx` still renders `HeroSection` before `KnowledgeExperienceCoordinator`, that the page now uses the shared rhythm or responsive grouping helper introduced in Plan 01, that `contact-section.tsx` still exposes the primary mailto CTA, and that `route-shell-skeleton.tsx` continues mirroring the recognizable page regions after the hierarchy rewrite. Keep the existing Node test runner in `package.json`; do not replace or expand the framework surface unless a minimal script update is truly required. The goal is objective regression coverage for layout contracts, not screenshot-driven verification.</action>
  <verify>npm run build && npm test</verify>
  <acceptance_criteria>
    - `tests/phase-04-knowledge-experience.test.ts` contains assertions covering `ResponsiveSectionGrid` or the new page-rhythm helper usage in `app/page.tsx`.
    - `tests/phase-04-knowledge-experience.test.ts` contains an assertion that `contact-section.tsx` still exposes the mailto primary CTA.
    - `package.json` still uses the Node test runner path and `npm test` passes after the new assertions are added.
  </acceptance_criteria>
  <done>Phase 10's responsive hierarchy contracts are enforced by the existing automated verification surface instead of living only in planning prose.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build && npm test
- [ ] node ".opencode/get-shit-done/bin/gsd-tools.cjs" verify references ".planning/phases/10-responsive-hierarchy-rewrite/10-02-PLAN.md"
- [ ] Supporting-section density, CTA visibility, and route-shell alignment are covered by source assertions.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| Lower supporting sections to recruiter-facing action path | Supporting-section density changes affect how obvious the contact destination remains | CTA layout, section spacing, responsive grouping |
| Test suite to layout contracts | Node tests verify hierarchy and loading-shell invariants against source files | File-level source assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Spoofing | `contact-section.tsx` and lower-page composition | mitigate | Keep the primary email CTA explicit and grouped with secondary contact actions so the supporting-section rewrite does not hide or dilute the main recruiter action. |
| T-10-04 | Tampering | `tests/phase-04-knowledge-experience.test.ts` and `route-shell-skeleton.tsx` | mitigate | Add concrete source assertions for page order, responsive grouping helpers, CTA wiring, and route-shell structure so future edits cannot silently undo the hierarchy rewrite. |
</threat_model>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Lower supporting sections read as compact secondary proof and support.
- Automated tests lock the responsive hierarchy rewrite into the repo's existing verification surface.

</success_criteria>

<output>
After completion, create the phase 10 plan 02 summary file in this phase directory.
</output>
