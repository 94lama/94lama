---
phase: 02-portfolio-page-experience
plan: "03"
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - app/page.tsx
  - app/globals.css
autonomous: true
requirements:
  - PORT-01
  - PORT-02
must_haves:
  truths:
    - The redesigned page stays comfortable on mobile and desktop with no broken one-page flow.
    - Responsive classes preserve the recruiter scan order established in the earlier plans.
    - The implementation ends with explicit automated and manual verification steps tied to the phase validation contract.
  artifacts:
    - path: app/page.tsx
      provides: Responsive layout classes and final single-page narrative polish.
      exports:
        - default
    - path: app/globals.css
      provides: Final supporting global style adjustments for the responsive portfolio shell.
      exports:
        - body base styles
  key_links:
    - from: app/page.tsx
      to: app/globals.css
      via: shared shell and responsive presentation
      pattern: className
---

<objective>
Finalize responsive behavior and verification readiness so the redesigned Phase 2 portfolio works cleanly across mobile and desktop and can pass both automated and human review.

Purpose: satisfy PORT-01 and reinforce PORT-02 by adding responsive polish, preserving the one-page narrative flow, and completing the source-level checks expected by the validation strategy.
Output: `app/page.tsx` and any required `app/globals.css` refinements ready for lint, build, and browser-based visual verification.
</objective>

<execution_context>
@/workspaces/94lama/.opencode/get-shit-done/workflows/execute-plan.md
@/workspaces/94lama/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-portfolio-page-experience/02-RESEARCH.md
@.planning/phases/02-portfolio-page-experience/02-VALIDATION.md
@.planning/phases/02-portfolio-page-experience/02-UI-SPEC.md
@app/page.tsx
@app/globals.css
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten responsive layout behavior for mobile and desktop scanning</name>
  <read_first>
    - app/page.tsx
    - app/globals.css
    - .planning/phases/02-portfolio-page-experience/02-RESEARCH.md
    - .planning/phases/02-portfolio-page-experience/02-UI-SPEC.md
  </read_first>
  <files>app/page.tsx, app/globals.css</files>
  <action>Review the redesigned `app/page.tsx` and add or adjust concrete responsive classes so the page remains comfortable around mobile width (~375px) and desktop width. Preserve the one-page narrative order established earlier; do not reorder sections differently per breakpoint. Ensure layouts that become columns on large screens still stack cleanly on mobile, avoid horizontal overflow, keep touch-target-sized interactive links where applicable, and use responsive spacing values consistent with the UI contract (`16/24/32/48/64px` rhythm). Only touch `app/globals.css` if a small supporting global rule is required for overflow, text balance, or shell polish.</action>
  <acceptance_criteria>
    - `app/page.tsx` contains responsive breakpoint classes including at least one `sm:`, one `lg:`, and one `xl:` token.
    - `app/page.tsx` contains `min-h-screen`.
    - `app/page.tsx` contains at least one grid or flex layout token used responsively.
    - `app/globals.css` does not reintroduce `Arial, Helvetica, sans-serif`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "sm:|lg:|xl:|min-h-screen|grid|flex" app/page.tsx &amp;&amp; ! rg -n "Arial, Helvetica, sans-serif" app/globals.css</automated>
  </verify>
  <done>The redesigned portfolio holds together as a single-page experience across target viewport sizes.</done>
</task>

<task type="auto">
  <name>Task 2: Run final automated checks and capture the required manual verification points</name>
  <read_first>
    - package.json
    - .planning/phases/02-portfolio-page-experience/02-VALIDATION.md
    - app/page.tsx
    - app/globals.css
  </read_first>
  <files>app/page.tsx, app/globals.css</files>
  <action>After responsive polish is complete, run the exact automated validation commands from the phase contract: `npm run lint` and `npm run build`. If either fails, fix the underlying source issues in `app/page.tsx` or `app/globals.css` before marking the plan complete. Then perform the manual browser checks described in `02-VALIDATION.md`: confirm the top viewport immediately communicates name, role, summary, and relocation context; confirm the page feels intentionally bold rather than starter-like; and confirm there is no horizontal scroll and the section order remains comfortable on mobile and desktop. Record the outcome in the execution summary for this plan.</action>
  <acceptance_criteria>
    - `npm run lint` exits 0.
    - `npm run build` exits 0.
    - The execution summary references manual checks for hero clarity, bold visual direction, and mobile/desktop comfort.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>The Phase 2 portfolio is verification-ready with both automated and manual quality checks completed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source code verification → release confidence | Build and lint results are used to judge whether the redesigned page is safe to ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-07 | D | responsive layout | mitigate | Verify narrow and wide layouts explicitly so the one-page portfolio cannot ship with unreadable or overflowing sections. |
| T-2-08 | T | final verification commands | mitigate | Use the repository's existing `npm run lint` and `npm run build` commands only; do not skip failing checks or rely on subjective visual approval alone. |
| T-2-09 | R | manual sign-off | mitigate | Capture the required manual review points in the execution summary so visual quality decisions are auditable after implementation. |
</threat_model>

<verification>
- `npm run lint`
- `npm run build`
- `rg -n "sm:|lg:|xl:|min-h-screen|grid|flex" app/page.tsx`
- `! rg -n "Arial, Helvetica, sans-serif" app/globals.css`
- Manual browser check at mobile and desktop widths for hero clarity, bold visual direction, and one-page scan flow
</verification>

<success_criteria>
- The redesigned page remains clean and readable on mobile and desktop.
- Automated validation passes with the repository's standard commands.
- Manual review confirms the Phase 2 output no longer feels like the default starter template.
</success_criteria>

<output>
After completion, create `.planning/phases/02-portfolio-page-experience/02-portfolio-page-experience-03-SUMMARY.md`
</output>
