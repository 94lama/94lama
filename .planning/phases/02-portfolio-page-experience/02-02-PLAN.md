---
phase: 02-portfolio-page-experience
plan: "02"
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - app/page.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - SKIL-01
  - EXPR-01
  - EXPR-02
  - PORT-02
must_haves:
  truths:
    - The hero immediately shows Riccardo's name, role, summary, and relocation context in a strong above-the-fold composition.
    - Skills, experience, education, and languages are reorganized into a deliberate recruiter-first narrative rather than flat starter sections.
    - The page still consumes `getPortfolioContent()` in an async server component and does not hardcode portfolio copy into the layout.
  artifacts:
    - path: app/page.tsx
      provides: The Phase 2 one-page portfolio composition and section hierarchy.
      exports:
        - default
  key_links:
    - from: app/page.tsx
      to: src/content/portfolio/get-portfolio-content.ts
      via: async server component import
      pattern: getPortfolioContent
---

<objective>
Transform the current simple renderer into the bold one-page portfolio experience described by the roadmap, research, and UI contract while keeping the Phase 1 content pipeline intact.

Purpose: satisfy HERO-01, HERO-02, HERO-03, SKIL-01, EXPR-01, EXPR-02, and PORT-02 through stronger composition, recruiter scan order, and section-specific presentation.
Output: `app/page.tsx` reorganized into an editorial, high-contrast portfolio layout that still renders entirely from `getPortfolioContent()`.
</objective>

<execution_context>
@/workspaces/94lama/.opencode/get-shit-done/workflows/execute-plan.md
@/workspaces/94lama/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-portfolio-page-experience/02-RESEARCH.md
@.planning/phases/02-portfolio-page-experience/02-VALIDATION.md
@.planning/phases/02-portfolio-page-experience/02-UI-SPEC.md
@.planning/phases/01-content-model-data-setup/01-content-model-data-setup-02-SUMMARY.md
@app/page.tsx
@src/content/portfolio/get-portfolio-content.ts
@src/content/portfolio/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the hero into a bold recruiter-first opening section</name>
  <read_first>
    - app/page.tsx
    - src/content/portfolio/get-portfolio-content.ts
    - src/content/portfolio/types.ts
    - .planning/phases/02-portfolio-page-experience/02-UI-SPEC.md
    - .planning/phases/02-portfolio-page-experience/02-RESEARCH.md
  </read_first>
  <files>app/page.tsx</files>
  <action>Refactor the top of `app/page.tsx` into a visually dominant hero that uses the existing server-side `content` object and keeps the page as an async Server Component. The hero must present, in the first viewport and in this order of emphasis, `content.hero.name`, `content.hero.role`, `content.summary`, and relocation/location context from `content.contact.location` plus `content.relocation.summary`. Use an editorial composition rather than the current plain vertical stack: combine oversized display text, compact metadata chips or labels, and one strong primary email CTA labeled exactly `Email Riccardo`. Keep all copy sourced from `content`; do not invent new hardcoded résumé claims beyond short static section labels and the approved CTA text.</action>
  <acceptance_criteria>
    - `app/page.tsx` contains `await getPortfolioContent()`.
    - `app/page.tsx` contains `Email Riccardo`.
    - `app/page.tsx` contains `content.hero.name`.
    - `app/page.tsx` contains `content.hero.role`.
    - `app/page.tsx` contains `content.summary`.
    - `app/page.tsx` contains `content.relocation.summary`.
    - `app/page.tsx` contains `content.contact.location`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "await getPortfolioContent\(|Email Riccardo|content\.hero\.name|content\.hero\.role|content\.summary|content\.relocation\.summary|content\.contact\.location" app/page.tsx</automated>
  </verify>
  <done>The page opens with a strong recruiter-first hero that makes positioning and relocation status instantly visible.</done>
</task>

<task type="auto">
  <name>Task 2: Recompose skills and credentials into scan-friendly editorial sections</name>
  <read_first>
    - app/page.tsx
    - src/content/portfolio/types.ts
    - .planning/phases/02-portfolio-page-experience/02-RESEARCH.md
    - .planning/phases/02-portfolio-page-experience/02-UI-SPEC.md
  </read_first>
  <files>app/page.tsx</files>
  <action>Replace the current uniform section treatment with differentiated recruiter-scan sections in `app/page.tsx`. Keep the narrative order locked to: hero/positioning, grouped skills, experience, education plus languages, relocation support, then contact support. Render skills as grouped category blocks using `content.skills.map(...)` and keep the frontend/backend/DevOps/database categories visually distinct. Give experience the highest visual weight after the hero by separating role, company, date range, and highlights into stronger cards or timeline-like articles. Pair education and languages into a coordinated supporting section so both remain on the page without competing with experience. Preserve the existing server-only data flow and avoid adding client components, animation libraries, or project showcase content.</action>
  <acceptance_criteria>
    - `app/page.tsx` contains `content.skills.map`.
    - `app/page.tsx` contains `content.experience.map`.
    - `app/page.tsx` contains `content.education.map`.
    - `app/page.tsx` contains `content.languages.map`.
    - `app/page.tsx` contains visible labels or headings for `Skills`, `Experience`, `Education`, `Languages`, `Relocation`, and `Contact`.
    - `app/page.tsx` does not contain `To get started, edit the page.tsx file.`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "content\.skills\.map|content\.experience\.map|content\.education\.map|content\.languages\.map|Skills|Experience|Education|Languages|Relocation|Contact" app/page.tsx &amp;&amp; ! rg -n "To get started, edit the page\.tsx file\." app/page.tsx</automated>
  </verify>
  <done>The portfolio content is reorganized into a clear, bold narrative that emphasizes recruiter scan speed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parsed portfolio content → JSX render | Typed résumé content is displayed directly in a richer page composition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-04 | T | `app/page.tsx` content render | mitigate | Continue rendering plain strings from the typed portfolio contract only; do not introduce `dangerouslySetInnerHTML`, markdown rendering, or client-side HTML injection. |
| T-2-05 | D | visual hierarchy | mitigate | Keep server-side render path intact and favor semantic headings, lists, and links so redesign work does not degrade usability or accessibility. |
| T-2-06 | I | public résumé/contact information | accept | Phase 2 continues to expose intentionally public portfolio information already present in the source CV. |
</threat_model>

<verification>
- `npm run lint`
- `rg -n "await getPortfolioContent\(|Email Riccardo|content\.hero\.name|content\.hero\.role|content\.summary|content\.relocation\.summary|content\.contact\.location" app/page.tsx`
- `rg -n "content\.skills\.map|content\.experience\.map|content\.education\.map|content\.languages\.map|Skills|Experience|Education|Languages|Relocation|Contact" app/page.tsx`
- `! rg -n "To get started, edit the page\.tsx file\." app/page.tsx`
</verification>

<success_criteria>
- The page hero satisfies the roadmap's immediate positioning and relocation-visibility goals.
- Skills and credentials are grouped in a stronger recruiter-friendly reading order.
- The redesign still uses the Phase 1 typed content pipeline with no client-side content fetching.
</success_criteria>

<output>
After completion, create `.planning/phases/02-portfolio-page-experience/02-portfolio-page-experience-02-SUMMARY.md`
</output>
