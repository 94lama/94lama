---
phase: 02-portfolio-page-experience
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - PORT-02
must_haves:
  truths:
    - The page shell uses the approved dark editorial palette and Geist font stack instead of the default starter styling.
    - The top viewport establishes a strong recruiter-first hierarchy for name, role, summary, and relocation availability.
    - Global layout and metadata continue to support a single-page server-rendered portfolio rather than a starter template.
  artifacts:
    - path: app/globals.css
      provides: Shared visual foundation for the approved Phase 2 color, type, and page-shell direction.
      exports:
        - :root theme variables
        - body base styles
    - path: app/layout.tsx
      provides: Global font/body wiring and portfolio metadata backing the redesigned page shell.
      exports:
        - metadata
        - default
  key_links:
    - from: app/layout.tsx
      to: app/globals.css
      via: global stylesheet import
      pattern: import "./globals.css"
---

<objective>
Replace the default global styling foundation with the approved editorial portfolio shell so the Phase 2 page can immediately feel bold, deliberate, and recruiter-focused.

Purpose: establish the dark/light panel system, Geist typography, metadata support, and above-the-fold design constraints required for HERO-01, HERO-02, HERO-03, and PORT-02 before section-level composition work.
Output: `app/globals.css` and `app/layout.tsx` aligned with the Phase 2 UI contract and ready for the redesigned one-page layout.
</objective>

<execution_context>
@/workspaces/94lama/.opencode/get-shit-done/workflows/execute-plan.md
@/workspaces/94lama/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-portfolio-page-experience/02-RESEARCH.md
@.planning/phases/02-portfolio-page-experience/02-VALIDATION.md
@.planning/phases/02-portfolio-page-experience/02-UI-SPEC.md
@.planning/phases/01-content-model-data-setup/01-content-model-data-setup-02-SUMMARY.md
@app/globals.css
@app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace starter global tokens with the Phase 2 editorial shell</name>
  <read_first>
    - app/globals.css
    - app/layout.tsx
    - .planning/phases/02-portfolio-page-experience/02-UI-SPEC.md
    - .planning/phases/02-portfolio-page-experience/02-RESEARCH.md
  </read_first>
  <files>app/globals.css</files>
  <action>Update `app/globals.css` so the page foundation follows the approved UI contract exactly: define root tokens for the dominant background `#0C0C0F`, secondary surface `#171A22`, accent `#B8FF20`, and readable foreground values; keep `--font-geist-sans` and `--font-geist-mono` wired through `@theme inline`; remove the fallback `Arial, Helvetica, sans-serif` body font and switch the body to the Geist Sans stack via `var(--font-geist-sans)`; give `body` a dark editorial baseline with the dominant background color, light foreground text, and a subtle non-starter feel using static gradients or texture-like layering that does not require JavaScript; preserve a usable light-mode fallback only if it comes essentially for free from the token structure, but optimize the page for the dark shell defined in the UI contract.</action>
  <acceptance_criteria>
    - `app/globals.css` contains `#0C0C0F`.
    - `app/globals.css` contains `#171A22`.
    - `app/globals.css` contains `#B8FF20`.
    - `app/globals.css` contains `var(--font-geist-sans)`.
    - `app/globals.css` no longer contains `Arial, Helvetica, sans-serif`.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "#0C0C0F|#171A22|#B8FF20|var\(--font-geist-sans\)" app/globals.css &amp;&amp; ! rg -n "Arial, Helvetica, sans-serif" app/globals.css</automated>
  </verify>
  <done>The app has a non-starter visual foundation that matches the approved portfolio design system.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten layout metadata and body classes around the new shell</name>
  <read_first>
    - app/layout.tsx
    - app/globals.css
    - .planning/phases/02-portfolio-page-experience/02-UI-SPEC.md
  </read_first>
  <files>app/layout.tsx</files>
  <action>Adjust `app/layout.tsx` only where needed to support the new portfolio shell: keep `Geist` and `Geist_Mono` as the only font families, preserve the portfolio metadata title/description, and ensure the `<body>` classes support a full-height single-page experience with the new dark shell and smooth text rendering. If a body class is added, it must be concrete and useful, such as `bg-background text-foreground` or an equivalent Tailwind 4 utility mapping to the updated CSS variables; do not add unrelated wrappers, scripts, or client logic.</action>
  <acceptance_criteria>
    - `app/layout.tsx` still imports `Geist` and `Geist_Mono` from `next/font/google`.
    - `app/layout.tsx` contains `Frontend-Focused Full-Stack Developer` in metadata.
    - `app/layout.tsx` contains a `<body className=` with at least `min-h-full` and one visual shell class.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Geist|Geist_Mono|Frontend-Focused Full-Stack Developer|<body className=.*min-h-full" app/layout.tsx</automated>
  </verify>
  <done>The root layout stays simple but now correctly hosts the new editorial portfolio foundation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-rendered portfolio shell → browser | Global visual tokens and metadata are delivered as static app code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-01 | T | `app/globals.css` theme shell | mitigate | Keep styling declarative in local CSS/Tailwind tokens only; do not inject runtime HTML, remote assets, or untrusted CSS sources. |
| T-2-02 | I | `app/layout.tsx` metadata | accept | Portfolio title/description are intentionally public marketing information; preserve only the intended public summary text. |
| T-2-03 | D | root shell readability | mitigate | Maintain sufficient contrast and keep the shell simple so styling changes cannot make content unreadable or inaccessible by default. |
</threat_model>

<verification>
- `npm run lint`
- `rg -n "#0C0C0F|#171A22|#B8FF20|var\(--font-geist-sans\)" app/globals.css`
- `! rg -n "Arial, Helvetica, sans-serif" app/globals.css`
- `rg -n "Geist|Geist_Mono|Frontend-Focused Full-Stack Developer|<body className=.*min-h-full" app/layout.tsx`
</verification>

<success_criteria>
- The app no longer relies on default starter-theme tokens and font fallbacks.
- The root shell visually supports a bold editorial portfolio direction.
- The page remains compatible with the existing server-rendered single-page structure.
</success_criteria>

<output>
After completion, create `.planning/phases/02-portfolio-page-experience/02-portfolio-page-experience-01-SUMMARY.md`
</output>
