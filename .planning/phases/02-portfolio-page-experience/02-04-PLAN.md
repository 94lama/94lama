---
phase: 02-portfolio-page-experience
plan: "04"
type: execute
wave: 3
depends_on:
  - 02-03
files_modified:
  - public/assets/cv.md
  - src/content/portfolio/types.ts
  - src/content/portfolio/parse-cv.ts
  - app/page.tsx
autonomous: true
gap_closure: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - PORT-02
must_haves:
  truths:
    - The first viewport shows Riccardo's name, frontend-focused role, one summary block, Italy location, EU relocation context, and a profile photo.
    - The hero no longer repeats summary, location, or relocation content across adjacent blocks.
    - The existing headshot is exposed through the shared content pipeline instead of being hardcoded directly in the page.
  artifacts:
    - path: public/assets/cv.md
      provides: Source-of-truth hero photo reference.
      contains: "Profile Photo:"
    - path: src/content/portfolio/types.ts
      provides: Typed hero photo contract shared by parser and page.
      exports:
        - HeroPhoto
        - HeroContent
    - path: src/content/portfolio/parse-cv.ts
      provides: Parsing of the authored hero photo line into typed content.
      exports:
        - parseCvMarkdown
    - path: app/page.tsx
      provides: Simplified hero composition with rendered headshot and deduplicated copy.
      exports:
        - default
  key_links:
    - from: public/assets/cv.md
      to: src/content/portfolio/parse-cv.ts
      via: authored hero-block `Profile Photo:` line
      pattern: "Profile Photo:"
    - from: src/content/portfolio/parse-cv.ts
      to: src/content/portfolio/types.ts
      via: `hero.photo` typed contract
      pattern: "photo"
    - from: app/page.tsx
      to: src/content/portfolio/get-portfolio-content.ts
      via: `content.hero.photo` consumption in the server component
      pattern: "content\\.hero\\.photo"
---

<objective>
Close the diagnosed Phase 2 UAT gap by removing redundant hero copy and adding a content-driven profile photo to the top of the portfolio page.

Purpose: restore immediate recruiter clarity for HERO-01, HERO-02, HERO-03, and PORT-02 without breaking the existing server-rendered one-page flow or the `public/assets/cv.md` source-of-truth decision.
Output: updated hero content contract/parser in `src/content/portfolio/*`, an authored photo reference in `public/assets/cv.md`, and a cleaner `app/page.tsx` hero that renders the existing headshot.
</objective>

<execution_context>
@/workspaces/94lama/.opencode/get-shit-done/workflows/execute-plan.md
@/workspaces/94lama/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-portfolio-page-experience/02-UAT.md
@.planning/phases/02-portfolio-page-experience/02-RESEARCH.md
@.planning/phases/02-portfolio-page-experience/02-portfolio-page-experience-02-SUMMARY.md
@.planning/phases/02-portfolio-page-experience/02-portfolio-page-experience-03-SUMMARY.md
@.planning/debug/hero-clarity-photo-gap.md
@public/assets/cv.md
@src/content/portfolio/types.ts
@src/content/portfolio/parse-cv.ts
@src/content/portfolio/get-portfolio-content.ts
@app/page.tsx

<interfaces>
From `src/content/portfolio/types.ts`:
```ts
export interface HeroContent {
  name: string;
  role: string;
}

export interface PortfolioContent {
  hero: HeroContent;
  summary: string;
  relocation: RelocationInfo;
  contact: ContactInfo;
}
```

From `src/content/portfolio/get-portfolio-content.ts`:
```ts
export async function getPortfolioContent(): Promise<PortfolioContent>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the shared portfolio contract to carry a hero photo per the diagnosed gap</name>
  <files>public/assets/cv.md, src/content/portfolio/types.ts, src/content/portfolio/parse-cv.ts</files>
  <action>Implement the content-pipeline half of the fix first so `app/page.tsx` can stay purely content-driven. In `public/assets/cv.md`, add exactly one authored hero-block line directly below the existing contact line: `Profile Photo: /assets/foto profilo.jpg`. In `src/content/portfolio/types.ts`, add an exported `HeroPhoto` interface and extend `HeroContent` with an optional `photo` field. In `src/content/portfolio/parse-cv.ts`, parse that authored line from the hero block before the first `##` section heading, stop relying on the current fixed `lines.slice(4)` assumption, and return `hero.photo` with a deterministic alt string derived from the hero name. Accept only a root-relative local asset path beginning with `/assets/`; if the authored value is missing or invalid, omit the photo instead of emitting a remote or malformed path. Keep `public/assets/cv.md` as the only maintained source of truth per the existing project decision, and do not hardcode the image path inside `app/page.tsx`.</action>
  <verify>
    <automated>rg -n "Profile Photo: /assets/foto profilo\\.jpg|export interface HeroPhoto|photo\\?:|photo:" public/assets/cv.md src/content/portfolio/types.ts src/content/portfolio/parse-cv.ts</automated>
  </verify>
  <done>The shared content model and parser expose an optional hero photo from `public/assets/cv.md`, ready for server-rendered page consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify the hero and render the existing headshot without regressing the one-page layout</name>
  <files>app/page.tsx</files>
  <action>Update `app/page.tsx` to consume `content.hero.photo` in the existing async Server Component. Import `Image` from `next/image` and render the headshot in the hero using a fixed-ratio, rounded frame with meaningful alt text, `priority`, and responsive `sizes` so the photo strengthens first-glance credibility without shifting layout. Reduce the hero copy to one primary summary block and one compact metadata area per the UAT gap: remove the duplicated recruiter snapshot summary, remove separate adjacent cards that repeat location and relocation already shown elsewhere in the hero, and keep location plus relocation in one compact metadata cluster. Preserve the required CTA text `Email Riccardo`, preserve the supporting knowledge-map section lower on the page, and keep the page server-rendered with no client-only logic or hardcoded résumé claims. The result must implement the gap exactly: less redundant hero text, plus the existing photo, while keeping the recruiter-first reading order intact.</action>
  <verify>
    <automated>test "$(rg -c "content\\.summary" app/page.tsx)" = "1" &amp;&amp; rg -n 'import Image from "next/image"|content\\.hero\\.photo|Email Riccardo' app/page.tsx &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>The hero shows a single clear summary, one compact metadata cluster, and the existing headshot while the full app still passes lint and build.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `public/assets/cv.md` → `parseCvMarkdown()` | Authored markdown hero metadata becomes typed content used by the UI |
| typed `hero.photo` → `app/page.tsx` | Parsed image metadata is rendered into the first viewport |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-10 | T | `src/content/portfolio/parse-cv.ts` photo path parsing | mitigate | Only accept root-relative local `/assets/...` image paths from the authored `Profile Photo:` line; drop invalid values instead of rendering arbitrary paths. |
| T-2-11 | I | `app/page.tsx` hero photo and metadata | accept | The headshot, location, and relocation details are intentional public portfolio content already destined for recruiter viewing. |
| T-2-12 | D | hero layout stability | mitigate | Render the photo in a fixed-ratio container with responsive sizing and keep the photo optional so missing/invalid data cannot break first-viewport readability. |
</threat_model>

<verification>
- `rg -n "Profile Photo: /assets/foto profilo\.jpg|export interface HeroPhoto|photo\?:|photo:" public/assets/cv.md src/content/portfolio/types.ts src/content/portfolio/parse-cv.ts`
- `test "$(rg -c "content\.summary" app/page.tsx)" = "1"`
- `rg -n 'import Image from "next/image"|content\.hero\.photo|Email Riccardo' app/page.tsx`
- `npm run lint`
- `npm run build`
</verification>

<success_criteria>
- The hero passes the failed UAT truth: name, role, summary, Italy location, EU relocation context, and photo are all immediately visible without redundant adjacent copy.
- The profile photo comes from the shared content pipeline, not a page-local constant.
- The portfolio remains a server-rendered single-page experience and still passes repository lint/build checks.
</success_criteria>

<output>
After completion, create `.planning/phases/02-portfolio-page-experience/02-portfolio-page-experience-04-SUMMARY.md`
</output>
