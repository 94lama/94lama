---
phase: 09-interactive-continuity-real-loading-states
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/knowledge-experience-coordinator.tsx
  - app/components/skills-knowledge-map.tsx
  - app/components/knowledge-map/knowledge-map-panels.tsx
  - app/components/knowledge-map/viewport.tsx
  - app/components/knowledge-map/runtime.ts
  - app/components/experience-timeline-section.tsx
  - app/components/experience-card.tsx
  - app/components/section-card-styles.ts
autonomous: true
requirements:
  - MOTN-04
  - LOAD-01
  - LOAD-02
user_setup: []
must_haves:
  truths:
    - The knowledge map, detail panel, and experience timeline keep one coordinated active state instead of changing independently.
    - The OGL viewport stays mounted during normal selection changes while visual emphasis updates to the next active item.
    - Any local placeholder or overlay in the interactive section is tied to real map readiness or pending continuity, not a timer-driven fake wait.
  artifacts:
    - path: app/components/knowledge-experience-coordinator.tsx
      provides: Shared active and pending continuity state for the interactive section.
      contains: pending
    - path: app/components/knowledge-map/viewport.tsx
      provides: Stable scene lifecycle separated from selection highlight sync.
      contains: createKnowledgeMapScene
    - path: app/components/knowledge-map/knowledge-map-panels.tsx
      provides: Stable panel and canvas shells with continuity overlays or ready-state treatment.
      contains: ready
    - path: app/components/experience-timeline-section.tsx
      provides: Full timeline continuity treatment while emphasis updates in place.
      contains: pending
  key_links:
    - from: app/components/knowledge-experience-coordinator.tsx
      to: app/components/skills-knowledge-map.tsx
      via: Shared selection and pending props flow from the coordinator into the interactive section.
      pattern: pending|selection
    - from: app/components/knowledge-map/viewport.tsx
      to: app/components/knowledge-map/runtime.ts
      via: Scene boot stays separate from syncHighlight updates so selection changes do not recreate the scene.
      pattern: syncHighlight|createKnowledgeMapScene
    - from: app/components/knowledge-experience-coordinator.tsx
      to: app/components/experience-timeline-section.tsx
      via: The same continuity state drives timeline helper copy or emphasis treatment.
      pattern: pending|helper|timeline
---

<objective>
Create the shared continuity plumbing for Phase 9 so the map, panel, and timeline preserve orientation through real pending changes without remounting the interactive section.

Purpose: Phase 9 needs one stable coordination path for active and pending state before route-level loading polish or verification guardrails can land cleanly.
Output: Coordinator-owned continuity state, a non-remounting map viewport lifecycle, and stable panel/timeline shells that keep context visible during real waits and state changes.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-interactive-continuity-real-loading-states/09-CONTEXT.md
@.planning/phases/09-interactive-continuity-real-loading-states/09-RESEARCH.md
@.planning/phases/09-interactive-continuity-real-loading-states/09-UI-SPEC.md
@app/components/knowledge-experience-coordinator.tsx
@app/components/skills-knowledge-map.tsx
@app/components/knowledge-map/knowledge-map-panels.tsx
@app/components/knowledge-map/viewport.tsx
@app/components/knowledge-map/runtime.ts
@app/components/experience-timeline-section.tsx
@app/components/experience-card.tsx
@app/components/section-card-styles.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the coordinator with shared continuity state for the map, panel, and timeline</name>
  <files>app/components/knowledge-experience-coordinator.tsx, app/components/skills-knowledge-map.tsx, app/components/knowledge-map/knowledge-map-panels.tsx, app/components/experience-timeline-section.tsx, app/components/experience-card.tsx, app/components/section-card-styles.ts</files>
  <read_first>app/components/knowledge-experience-coordinator.tsx, app/components/skills-knowledge-map.tsx, app/components/knowledge-map/knowledge-map-panels.tsx, app/components/experience-timeline-section.tsx, app/components/experience-card.tsx, app/components/section-card-styles.ts, .planning/phases/09-interactive-continuity-real-loading-states/09-CONTEXT.md, .planning/phases/09-interactive-continuity-real-loading-states/09-UI-SPEC.md</read_first>
  <action>Keep `KnowledgeExperienceCoordinator` as the single owner for both the current selection and any short-lived pending continuity state described in D-04 through D-09. Add explicit continuity props that let `SkillsKnowledgeMap`, `KnowledgeMapDetailsPanel`, and `ExperienceTimelineSection` preserve their shells while the next active item is being resolved. Keep the previous panel copy or timeline emphasis visible long enough to preserve orientation, then transition the next state into the same panel and timeline slots. Update `section-card-styles.ts` only as needed to add shared continuity surface classes for dimming, overlay, or emphasis changes. Do not filter the timeline down to matches only, do not replace the full panel shell with a separate loader, and do not move responsive layout responsibilities into this phase.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `app/components/knowledge-experience-coordinator.tsx` contains explicit continuity or pending state alongside the active selection state.
    - `app/components/knowledge-map/knowledge-map-panels.tsx` contains continuity-related panel or canvas shell treatment without removing the existing shell structure.
    - `app/components/experience-timeline-section.tsx` still renders all entries and contains continuity-related props or state handling rather than matched-only filtering.
  </acceptance_criteria>
  <done>The interactive section has one shared continuity path in code, and the panel plus timeline keep stable landmarks while active-state changes are applied in place.</done>
</task>

<task type="auto">
  <name>Task 2: Stop remounting the OGL scene on selection changes and bind local loading to real readiness</name>
  <files>app/components/skills-knowledge-map.tsx, app/components/knowledge-map/viewport.tsx, app/components/knowledge-map/runtime.ts, app/components/knowledge-map/knowledge-map-panels.tsx</files>
  <read_first>app/components/skills-knowledge-map.tsx, app/components/knowledge-map/viewport.tsx, app/components/knowledge-map/runtime.ts, app/components/knowledge-map/knowledge-map-panels.tsx, .planning/phases/09-interactive-continuity-real-loading-states/09-RESEARCH.md</read_first>
  <action>Refactor the map viewport lifecycle so `createKnowledgeMapScene()` runs when the scene actually needs to boot, not whenever `selectedNodeId` changes. Keep `syncHighlight()` as the selection-driven update path. Thread a real ready-state handshake from `KnowledgeMapViewport` back into `SkillsKnowledgeMap` or the panel shell so the map area can show a reserved canvas placeholder, overlay, or handoff treatment only until the scene is ready. Keep the canvas shell mounted, keep reduced-motion branching intact, and avoid any timer-driven fake loading. The final behavior should preserve the viewport shell through normal hotspot changes while only the highlight and continuity state change.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `app/components/knowledge-map/viewport.tsx` no longer recreates the scene from an effect dependency on `selectedNodeId`.
    - `app/components/knowledge-map/runtime.ts` still exports `createKnowledgeMapScene` and `syncHighlight` as separate lifecycle concerns.
    - `app/components/skills-knowledge-map.tsx` or `app/components/knowledge-map/knowledge-map-panels.tsx` contains an explicit ready or loading handshake tied to the viewport shell.
  </acceptance_criteria>
  <done>The OGL scene lifecycle stays stable during normal selection changes, and any map loading treatment is tied to real scene readiness instead of a synthetic delay.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build
- [ ] gsd-tools verify references passes for this plan file
- [ ] The coordinator, viewport, panel, and timeline all use one continuity path in code without viewport remounts on selection changes.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| Client state to interactive UI surfaces | Shared continuity state flows from the coordinator into the map, panel, and timeline shells | Selection ids, helper copy, pending flags |
| Client UI to OGL runtime | Viewport lifecycle and highlight updates control the canvas scene | Selected node ids, ready-state callbacks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01 | Tampering | `KnowledgeExperienceCoordinator` continuity state | mitigate | Keep one coordinator-owned source of truth for active and pending selection state instead of divergent local state in each surface. |
| T-9-02 | Denial of Service | `KnowledgeMapViewport` / `runtime.ts` lifecycle | mitigate | Remove `selectedNodeId` from scene-creation dependencies so normal selection changes do not recreate the OGL scene or cause avoidable rendering churn. |
| T-9-03 | Spoofing | Interactive loading treatment | mitigate | Bind placeholders or overlays only to real map readiness and keep stable shells visible so the UI does not imply false loading states. |
</threat_model>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The interactive section preserves context through one shared continuity owner instead of per-surface ad hoc transitions.
- The map viewport remains mounted on normal selection changes while the panel and timeline update in place.

</success_criteria>

<output>
After completion, create the phase 9 plan 01 summary file in this phase directory.
</output>
