---
phase: 09-interactive-continuity-real-loading-states
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/loading.tsx
  - app/page.tsx
  - app/components/loading/route-shell-skeleton.tsx
  - tests/phase-04-knowledge-experience.test.ts
  - package.json
autonomous: true
requirements:
  - LOAD-01
  - LOAD-02
  - LOAD-03
user_setup: []
must_haves:
  truths:
    - Route-level loading preserves the page's recognizable structure instead of showing a generic loading screen.
    - Real-loading guardrails stay explicit in code and tests so immediately available recruiter content is not replaced with fake placeholders.
    - Automated verification protects the interactive continuity wiring from regressing back to remounts or fake loading behavior.
  artifacts:
    - path: app/loading.tsx
      provides: Route-level loading entrypoint for the portfolio page.
      contains: route-shell
    - path: app/components/loading/route-shell-skeleton.tsx
      provides: Structural loading shell that mirrors the page layout.
      contains: Hero
    - path: tests/phase-04-knowledge-experience.test.ts
      provides: Continuity and loading guardrail coverage in the existing Node test suite.
      contains: loading
  key_links:
    - from: app/loading.tsx
      to: app/components/loading/route-shell-skeleton.tsx
      via: The App Router loading entrypoint renders the structural route shell component.
      pattern: route-shell-skeleton|RouteShell
    - from: tests/phase-04-knowledge-experience.test.ts
      to: app/components/knowledge-map/viewport.tsx
      via: Tests assert the viewport lifecycle no longer recreates the scene on `selectedNodeId` changes.
      pattern: selectedNodeId|createKnowledgeMapScene|syncHighlight
    - from: tests/phase-04-knowledge-experience.test.ts
      to: app/loading.tsx
      via: Tests assert the route-level loading shell exists and mirrors portfolio structure.
      pattern: loading|route-shell
---

<objective>
Add the route-level loading shell and lock Phase 9 continuity guardrails into the existing verification surface.

Purpose: After the interactive continuity plumbing lands, the phase still needs a truthful route-level fallback and automated coverage that proves fake loading and remount regressions stay out.
Output: A structural app loading shell plus Node tests that verify the viewport lifecycle and real-loading boundaries.
</objective>

<execution_context>
@.opencode/get-shit-done/workflows/execute-plan.md
@.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-interactive-continuity-real-loading-states/09-CONTEXT.md
@.planning/phases/09-interactive-continuity-real-loading-states/09-RESEARCH.md
@.planning/phases/09-interactive-continuity-real-loading-states/09-UI-SPEC.md
@.planning/phases/09-interactive-continuity-real-loading-states/09-01-PLAN.md
@app/page.tsx
@app/layout.tsx
@tests/phase-04-knowledge-experience.test.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a structural route-level loading shell that mirrors the portfolio page</name>
  <files>app/loading.tsx, app/components/loading/route-shell-skeleton.tsx, app/page.tsx</files>
  <read_first>app/page.tsx, app/layout.tsx, .planning/phases/09-interactive-continuity-real-loading-states/09-UI-SPEC.md, .planning/phases/09-interactive-continuity-real-loading-states/09-CONTEXT.md</read_first>
  <action>Create app/loading.tsx and a small loading-shell component under app/components/loading/ that mirrors the real page structure from `app/page.tsx`: hero region first, the interactive map/timeline region second, then the two lower content grids. Keep placeholder geometry close to the real sections and keep motion restrained. Do not add interactive fake controls, generic centered spinner-only UI, or placeholders for every piece of server-rendered copy. The shell should preserve recognizable structure during real page-start or navigation waits without changing the root layout ownership in `app/layout.tsx`.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - app/loading.tsx exists and renders a route-level loading shell component.
    - app/components/loading/route-shell-skeleton.tsx exists and contains recognizable hero, interactive-section, and lower-grid structure.
    - `app/layout.tsx` remains the root shell owner and is not repurposed into a loading implementation file.
  </acceptance_criteria>
  <done>The portfolio has a route-level loading fallback that looks like the same page arriving, not a generic application placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the existing Node tests to lock continuity and real-loading guardrails</name>
  <files>tests/phase-04-knowledge-experience.test.ts, package.json</files>
  <read_first>tests/phase-04-knowledge-experience.test.ts, package.json, app/components/knowledge-map/viewport.tsx, app/page.tsx, .planning/phases/09-interactive-continuity-real-loading-states/09-RESEARCH.md</read_first>
  <action>Extend `tests/phase-04-knowledge-experience.test.ts` with concrete assertions for Phase 9 guardrails. Add checks that `viewport.tsx` no longer recreates the scene from a `selectedNodeId` dependency, that the route-level loading entrypoint exists, and that the route shell preserves recognizable page regions instead of a spinner-only fallback. Keep the existing Node test runner in `package.json`; do not replace it with a different framework. If the current npm test script needs an additional file path, update it minimally so the new assertions run as part of the existing suite.</action>
  <verify>npm run build && npm test</verify>
  <acceptance_criteria>
    - `tests/phase-04-knowledge-experience.test.ts` contains assertions covering the route-level loading entrypoint and the viewport lifecycle.
    - `tests/phase-04-knowledge-experience.test.ts` contains an assertion that `selectedNodeId` is not part of the scene-creation effect dependencies in `app/components/knowledge-map/viewport.tsx`.
    - `package.json` still uses the Node test runner path and `npm test` passes after the new assertions are added.
  </acceptance_criteria>
  <done>Phase 9's continuity and real-loading rules are enforced by the existing automated verification surface instead of living only in prose.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm run build && npm test
- [ ] gsd-tools verify references passes for this plan file
- [ ] Route-level loading and continuity guardrails are covered by automated tests and do not require framework churn.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| App Router loading fallback to recruiter UI | Route-level loading shell stands in for the portfolio page during real waits | Structural placeholder content |
| Test suite to implementation contracts | Node tests verify continuity and loading invariants against app source files | File-level source assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-04 | Spoofing | app/loading.tsx route shell | mitigate | Mirror the actual page structure and avoid spinner-only or misleading interactive placeholder content so users are not shown a false screen state. |
| T-9-05 | Tampering | Phase 9 verification coverage | mitigate | Add explicit source assertions for viewport lifecycle and loading shell wiring so later changes cannot silently reintroduce fake loading or remounts. |
</threat_model>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Route-level loading preserves the page's structure during real waits without fake content-wide skeletonization.
- Automated tests cover the key Phase 9 continuity and real-loading invariants.

</success_criteria>

<output>
After completion, create the phase 9 plan 02 summary file in this phase directory.
</output>
